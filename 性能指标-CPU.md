---
tags:
  - Areas/基础概念
  - Areas/Coder/javaWeb
category: 思维
status: 加工
project:
application:
source:
---
>**笔记不是为了复述书本，而是为了**“存档当下的自己”。如果你的笔记里没有你的思考痕迹、痛苦经历和选择理由**，它就只是一份毫无生命力的说明书，自然无法在未来唤醒你的认知***


## 💥 核心结论
>核心定义是什么? 核心价值在哪里?

### 🟣 核心定义：服务器干活的核心，主要用于「计算」和「线程/资源管理」；等待IO操作时不工作。
### 🟣核心价值：衡量服务器工作忙不忙的指标

## 🔪 我的见解
>为什么要记录它「我遇到了什么问题」？它能解决哪些痛点？

### 🟣为什么要记录它？
- 搞不清楚应用程序与CPU之间的作用关系
- 不知道怎么查CPU使用情况（应用程序、整机）
- 混淆了「**CPU%**」和「**CPU使用情况**」概念
## ⚡️ 我的重构
>它的底层逻辑是什么？（尝试用最简单的类比解释给外行听）它的结构是什么?

### 🟣结构组成：
- us（用户态）
	- 定义：描述应用程序代码消耗的CPU情况（业务逻辑、循环、JSON解析、**GC**等）
- sy（系统）
	- 定义：描述操作系统管理线程/资源时消耗的CPU情况（线程切换、锁竞争、CAS自旋、销毁/创建）

### 🟣概念区分：
- **CPU%**：表示单个进程占用，单核100%**累加**计算。
- **CPU使用情况**：整机所有核心平均负载，<70%最佳。

### 🟣 **总结：非计算密集型系统，用户态的CPU消耗会低（IO操作不消耗CPU）、但系统态CPU消耗不一定会低（多线程并发工作、GC频繁）**
## 🚀 实践应用：

### 🟣CPU查询操作
- 查询CPU核数 `grep -c processor /proc/cpuinfo`
- 查询CPU负载（LOAD）情况（与CPU核数对齐）`uptime`
```bash
load average: 0.98, 1.03, 0.87
----------------------------------
0.98：一分钟平均负载
1.03：五分钟平均负载
0.87：十五分钟平均负载（不重要）
反映当前有多少任务等着CPU处理，值⬆️
```
- 查询CPU使用情况 `top`：
1. 整机CPU使用情况（%）
```bash
%Cpu(s): 5.8 us, 1.2 sy, 0.0 ni, 92.5 id, 0.0 wa, 0.0 hi, 0.0 si, 0.0 st
----------------------------------------
us：用户态CPU使用情况（5.8%）
sy：系统态CPU使用情况（1.2%）
id（idle）：CPU空闲情况（92.5%）
```
2. 进程CPU使用情况
```bash
PID USER      PR  NI    VIRT    RES  SHR S  %CPU  %MEM     TIME+ COMMAND
1234 root      20   0  212344  56672  8224 S  185.7  1.4   123:45 java
---------------------------------------
java单程序使用的核数情况
```
