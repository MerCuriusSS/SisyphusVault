# Java 性能优化

## 0. 总体方法论：先定位，再优化，最后验证

### 回答要点

- 性能优化不是凭感觉改代码，而是基于指标定位瓶颈。
- 先明确目标：降低 RT、提升 QPS、降低 CPU、减少 GC、降低数据库压力。
- 再通过监控、日志、压测、链路追踪定位瓶颈。
- 然后按数据库、缓存、JVM、代码、并发、网络、架构分层优化。
- 最后通过同样的压测模型和线上指标验证效果。

### 常见目标

- 接口响应时间从 2s 降到 300ms。
- P95/P99 响应时间下降。
- QPS 提升。
- 慢 SQL 减少。
- CPU 使用率下降。
- Full GC 次数减少。
- Redis 命中率提升。
- MQ 堆积减少。

### 工具/指标

- 压测：JMeter、wrk、ab、Gatling、K6。
- 监控：Prometheus、Grafana、Micrometer、Actuator。
- 链路追踪：SkyWalking、Zipkin、Jaeger、Pinpoint。
- 日志：traceId、接口耗时日志、慢 SQL 日志、错误日志。
- JVM：GC 日志、jstat、jmap、jstack、Arthas、VisualVM、MAT。
- 数据库：慢查询日志、explain、show processlist、数据库监控。
- Profiling：async-profiler、火焰图、JFR。

### 串联方式

- 先说方法论，避免直接堆优化手段。
- 然后说：“我会先看链路耗时，把问题定位到数据库、JVM、线程池或外部服务。”
- 再按瓶颈所在层展开具体优化。
- 最后说：“优化后会用 P95/P99、QPS、错误率、资源使用率验证。”

## 1. 数据库优化：最常见的后端性能瓶颈

### 回答要点

- 很多 Java 接口慢，根因不在 Java 代码，而在数据库访问。
- 数据库优化优先看 SQL 是否慢、索引是否命中、扫描行数是否过大。
- 还要看是否存在 N+1 查询、大事务、深分页、连接池不足等问题。
- 优化 SQL 前要看执行计划，不能只凭感觉加索引。

### 常见问题

- 慢 SQL。
- 全表扫描。
- 索引失效。
- N+1 查询。
- 深分页慢。
- join 过重。
- select * 返回字段过多。
- 单次查询数据量过大。
- 大事务、长事务。
- 锁等待、死锁。
- 数据库连接池打满。

### 具体优化手段

#### SQL 优化

- 避免 select *。
- 只查询必要字段，使用 DTO 投影。
- 避免在 where 条件字段上使用函数导致索引失效。
- 避免隐式类型转换。
- 避免不必要的 order by、group by。
- 减少复杂 join，必要时拆分查询。
- 批量查询代替循环单条查询。

#### 索引优化

- 为 where、join、order by 高频字段建立索引。
- 根据查询条件设计联合索引。
- 遵守最左前缀原则。
- 高区分度字段更适合建索引。
- 使用覆盖索引减少回表。
- 避免索引过多影响写入性能。
- 定期清理无效或重复索引。

#### 分页优化

- 小数据量可以 limit offset。
- 大 offset 深分页改成游标分页。
- 使用 `where id > lastId order by id limit size`。
- 先查主键 ID，再回表查询完整数据。
- 搜索型分页可交给 Elasticsearch。

#### ORM/JPA/MyBatis 优化

- 解决 N+1 查询。
- 使用 join fetch 或批量查询。
- 控制懒加载触发时机。
- 分页查询避免把全部数据加载到内存。
- 批量插入、批量更新。
- 避免在循环里调用 mapper/repository。

#### 事务优化

- 缩小事务范围。
- 避免事务中调用远程服务。
- 避免事务中做大量计算或文件 IO。
- 降低锁持有时间。
- 合理选择事务隔离级别。
- 拆分大事务。

#### 架构级数据库优化

- 读写分离。
- 分库分表。
- 冷热数据分离。
- 历史数据归档。
- 预聚合统计结果。
- 报表查询走 OLAP 或离线任务。

### 工具/指标

- 慢 SQL 日志。
- explain 执行计划。
- rows 扫描行数。
- type 访问类型。
- possible_keys / key 是否命中索引。
- Extra 中是否出现 filesort、temporary。
- 数据库 CPU、IO、连接数。
- HikariCP 活跃连接数、等待连接数。
- MyBatis/JPA SQL 日志。

### 串联方式

- “如果链路追踪显示耗时主要在数据库，我会先看慢 SQL 和 explain。”
- “如果是扫描行数大，就考虑索引和 SQL 改写。”
- “如果 SQL 次数多，就排查 N+1 和循环查库。”
- “如果数据库压力高但数据读多写少，可以引入缓存。”
- “如果单库数据量或写入压力太大，再考虑读写分离、分库分表。”

## 2. 缓存优化：降低数据库压力和接口响应时间

### 回答要点

- 缓存适合读多写少、热点明显、允许最终一致性的场景。
- 缓存的目标是减少数据库访问、降低接口 RT、提升吞吐。
- 不能无脑加缓存，要考虑一致性、失效策略和异常场景。
- 重点要能讲清楚穿透、击穿、雪崩、热点 Key。

### 常见问题

- 热点数据频繁查数据库。
- 数据库读压力过高。
- 接口响应慢。
- 相同计算结果重复生成。
- 缓存命中率低。
- 缓存穿透。
- 缓存击穿。
- 缓存雪崩。
- 热点 Key 打爆 Redis 单节点。
- 缓存与数据库不一致。

### 具体优化手段

#### 缓存设计

- Redis 分布式缓存。
- Caffeine/Guava 本地缓存。
- 本地缓存 + Redis 多级缓存。
- 缓存预热。
- 缓存定时刷新。
- 热点数据常驻缓存。
- 合理设置 TTL。

#### 缓存穿透

- 参数合法性校验。
- 布隆过滤器。
- 缓存空值。
- 对恶意请求做限流。

#### 缓存击穿

- 互斥锁。
- 逻辑过期。
- 异步刷新。
- 热点 Key 不设置物理过期。
- 单飞机制，避免多个线程同时回源。

#### 缓存雪崩

- TTL 加随机值。
- 不同类型数据错开过期时间。
- Redis 高可用。
- 本地缓存兜底。
- 限流降级。
- 熔断保护数据库。

#### 热点 Key

- 热点 Key 拆分。
- 本地缓存抗热点。
- 读请求副本分摊。
- 提前识别热点数据。
- 限制单 Key 高频访问。

#### 一致性

- 先更新数据库，再删除缓存。
- 删除缓存失败要重试。
- 延迟双删。
- MQ 异步删除或刷新缓存。
- 订阅 binlog 同步缓存。
- 根据业务决定强一致还是最终一致。

### 工具/指标

- Redis 命中率。
- Redis QPS。
- Redis 慢日志 slowlog。
- Redis 内存使用。
- Redis 大 Key、热 Key。
- 缓存 key 数量。
- 数据库 QPS 是否下降。
- 接口 P95/P99 是否下降。
- 缓存回源次数。

### 串联方式

- “当数据库读压力高，并且数据读多写少时，我会考虑缓存。”
- “加缓存前先判断业务是否允许短暂不一致。”
- “加缓存后要监控命中率，而不是只看接口变快没变快。”
- “如果命中率低，要分析 key 设计、TTL 和访问模式。”
- “缓存异常时要有限流、降级或本地缓存兜底，避免数据库被打穿。”

## 3. JVM 优化：关注 GC、内存和线程状态

### 回答要点

- JVM 优化不是一上来调参数，而是先看 GC、内存、线程和对象分配。
- 重点关注 Full GC、内存泄漏、对象创建过多、线程阻塞。
- 先通过日志和 dump 定位原因，再考虑代码优化或参数调整。
- JVM 优化通常和代码对象生命周期、缓存大小、线程池配置有关。

### 常见问题

- Full GC 频繁。
- Stop The World 时间长。
- Old 区持续增长。
- 内存泄漏。
- 大对象频繁分配。
- 对象创建过多。
- 元空间溢出。
- 线程阻塞。
- 死锁。
- CPU 飙高。

### 具体优化手段

#### GC 优化

- 分析 GC 日志，确认 Young GC、Mixed GC、Full GC 情况。
- 减少短时间大量对象创建。
- 避免大对象频繁进入老年代。
- 控制缓存、集合、队列大小。
- 修复对象无法释放的问题。
- 合理设置堆大小。
- 根据 JDK 版本和场景选择 G1、ZGC 等收集器。

#### 内存泄漏优化

- dump 堆内存。
- 分析大对象和引用链。
- 检查静态集合未清理。
- 检查 ThreadLocal 未 remove。
- 检查监听器、回调、缓存未释放。
- 限制本地缓存和队列容量。

#### CPU 飙高优化

- top 定位高 CPU 进程。
- top -H 定位高 CPU 线程。
- jstack 查看线程栈。
- 转换线程 ID 找到对应 Java 线程。
- 排查死循环、频繁 GC、复杂正则、热点计算。
- 用火焰图分析热点方法。

#### 参数优化

- `-Xms` / `-Xmx` 设置堆大小。
- `-XX:MaxMetaspaceSize` 设置元空间上限。
- 启用 GC 日志。
- 设置 OOM 自动 dump。
- G1 下关注 MaxGCPauseMillis。
- 参数调整必须结合压测验证。

### 工具/指标

- jstat：查看 GC 次数和耗时。
- jmap：dump 堆内存。
- jstack：查看线程栈。
- jcmd：查看 JVM 信息和触发诊断命令。
- Arthas：线上诊断方法耗时、watch、trace、thread、heapdump。
- VisualVM：本地可视化分析。
- MAT：分析 heap dump 和引用链。
- JFR：低开销运行时分析。
- async-profiler：CPU/内存火焰图。
- GC 日志：分析停顿、频率、原因。
- 指标：堆使用率、Full GC 次数、GC pause、对象分配速率、线程数。

### 串联方式

- “如果接口整体抖动，而不是某个 SQL 慢，我会看 JVM 和系统资源。”
- “如果 P99 偶尔很高，可能和 GC 停顿、线程阻塞、外部调用超时有关。”
- “如果 Old 区持续上涨，就 dump 堆分析引用链。”
- “如果 CPU 高，就结合线程栈和火焰图找热点方法。”
- “JVM 参数调整是最后一步，优先修复代码和资源使用问题。”

## 4. 代码层优化：减少重复工作和无效消耗

### 回答要点

- 代码优化的核心不是纠结语法微优化，而是减少无效工作。
- 重点看是否有循环查库、重复计算、不合理集合、大对象创建、阻塞调用。
- 高频接口上的序列化、对象拷贝、日志打印也可能成为瓶颈。
- 优化代码要关注复杂度、数据量和调用次数。

### 常见问题

- 循环里查数据库。
- 循环里调 RPC。
- 重复计算。
- List contains 大量调用导致 O(n²)。
- 大集合一次性加载进内存。
- 大文件一次性读取。
- Bean copy 过度使用。
- JSON 序列化对象过大。
- 日志打印大对象。
- 正则表达式性能差。
- 同步阻塞代码过多。

### 具体优化手段

#### 批量化

- 批量查询代替循环查询。
- 批量插入代替逐条插入。
- 批量 RPC 代替多次 RPC。
- 批量组装数据减少网络和数据库往返。

#### 降低复杂度

- List contains 改为 HashSet contains。
- 双重循环改为 Map 分组。
- 提前构建索引 Map。
- 避免重复排序。
- 避免重复过滤。

#### 减少对象和内存压力

- 避免无意义临时对象。
- 控制集合初始容量。
- 大文件流式处理。
- 分页处理大数据。
- 减少深拷贝。
- 谨慎使用 BeanUtils 高频反射拷贝。

#### 序列化和日志

- 接口只返回必要字段。
- 避免返回大对象树。
- 高频日志降级或采样。
- 日志参数化，避免无效字符串拼接。
- 避免打印超大 JSON。
- 必要时优化 JSON 序列化框架配置。

### 工具/指标

- Arthas trace：查看方法调用耗时。
- Arthas watch：观察方法入参返回值。
- Arthas monitor：统计方法调用次数和耗时。
- async-profiler：找 CPU 热点。
- JFR：分析对象分配和热点方法。
- 单元压测/JMH：微基准测试。
- 日志中的方法耗时。
- 指标：方法耗时、调用次数、对象分配率、内存占用。

### 串联方式

- “如果 SQL 不慢但接口仍然慢，我会看应用内部方法耗时。”
- “如果某个方法调用次数很多，就看是否能批量化或缓存结果。”
- “如果 CPU 高，就用火焰图看热点代码。”
- “如果内存压力大，就看对象分配和集合大小。”
- “代码优化完成后，要再看接口 RT 和资源指标是否改善。”

## 5. 并发与线程池优化：控制资源，而不是盲目加线程

### 回答要点

- 线程池用于复用线程和控制并发，不是线程越多性能越好。
- CPU 密集型和 IO 密集型任务的线程数设置思路不同。
- 要监控活跃线程数、队列长度、拒绝次数和任务耗时。
- 锁优化重点是缩小锁粒度，避免锁内做慢操作。
- 并发优化要配合限流、隔离和降级，避免资源被打满。

### 常见问题

- 线程池队列堆积。
- 线程数过多导致上下文切换严重。
- 线程池被慢任务占满。
- 拒绝策略不合理。
- 锁竞争严重。
- 死锁。
- 接口同步等待多个外部结果。
- 异步任务没有资源隔离。
- CompletableFuture 共用公共线程池导致互相影响。

### 具体优化手段

#### 线程池配置

- 根据任务类型设置核心线程数。
- CPU 密集型接近 CPU 核数。
- IO 密集型可适当增大线程数。
- 设置有界队列，避免无限堆积。
- 设置合理拒绝策略。
- 为不同业务配置独立线程池。
- 线程池参数要结合压测调整。

#### 异步化

- 非核心流程异步执行。
- 日志、通知、短信、邮件异步化。
- 多个独立远程调用并行执行。
- 使用 CompletableFuture 指定自定义线程池。
- 注意超时控制。
- 注意异常处理和结果回收。

#### 锁优化

- 缩小锁范围。
- 降低锁粒度。
- 避免锁内调用数据库或 RPC。
- 读多写少使用读写锁。
- 高频计数使用 LongAdder。
- 使用无锁数据结构。
- 分段锁降低竞争。

#### 资源隔离

- 核心业务和非核心业务线程池隔离。
- 不同下游依赖调用池隔离。
- 慢任务和快任务隔离。
- 设置超时和熔断。
- 防止一个依赖拖垮整个服务。

### 工具/指标

- 线程池活跃线程数。
- 线程池队列长度。
- 任务执行耗时。
- 任务等待时间。
- 拒绝次数。
- jstack 线程状态。
- Arthas thread。
- CPU 上下文切换。
- 锁等待时间。
- 死锁检测。

### 串联方式

- “如果接口请求堆积，我会看线程池是否打满。”
- “如果线程池打满，要判断是线程数太少、队列太小，还是任务本身太慢。”
- “如果任务慢，要继续向下查数据库、RPC 或锁竞争。”
- “如果是外部依赖慢，要做超时、熔断、隔离。”
- “如果是非核心流程阻塞主链路，可以异步化或 MQ 化。”

## 6. 接口与远程调用优化：减少链路耗时和故障放大

### 回答要点

- 微服务系统中，接口慢经常来自远程调用和链路过长。
- 要关注调用次数、单次耗时、超时、重试、连接池和响应体大小。
- 外部调用必须设置超时，重试要有限制并配合熔断。
- 非核心调用可以异步化，核心调用要保证稳定性。

### 常见问题

- 一个接口调用多个下游服务。
- 串行调用导致总耗时累加。
- 下游服务偶发超时。
- 重试放大流量。
- 响应数据过大。
- HTTP 连接池不足。
- 未设置超时时间。
- 第三方接口拖慢主流程。
- 网关或服务间链路过长。

### 具体优化手段

#### 接口设计

- 减少返回字段。
- 分页返回。
- 批量接口减少多次请求。
- 合并强相关接口。
- 避免接口返回过深对象结构。
- 使用压缩降低大响应传输成本。

#### 远程调用

- 设置连接超时和读取超时。
- 设置连接池。
- 合理配置最大连接数。
- 独立下游调用可以并行。
- 下游慢时快速失败。
- 对第三方接口做隔离。

#### 稳定性保护

- 限流。
- 熔断。
- 降级。
- 舱壁隔离。
- 重试退避。
- 幂等设计。
- 防止重试风暴。

### 工具/指标

- 链路追踪中的每段调用耗时。
- 下游接口 P95/P99。
- 超时次数。
- 重试次数。
- 熔断次数。
- 降级次数。
- HTTP 连接池使用率。
- 网关耗时。
- 请求体/响应体大小。
- 错误率。

### 串联方式

- “如果链路追踪显示耗时在下游服务，就看下游 RT 和超时配置。”
- “如果多个下游互不依赖，可以并行调用。”
- “如果下游不稳定，要加超时、熔断、降级。”
- “如果非核心流程依赖第三方接口，可以异步化或 MQ 化。”
- “如果响应体太大，就优化 DTO 和分页。”

## 7. MQ 与异步化优化：削峰、解耦、保护主链路

### 回答要点

- MQ 不一定让单个任务更快，但能削峰、解耦、提升系统吞吐和稳定性。
- 适合非实时、可最终一致、可异步处理的场景。
- 使用 MQ 后要关注消息可靠性、幂等、顺序、重复消费和堆积。
- 异步化的核心是缩短主链路耗时，把非核心动作移出同步流程。

### 常见问题

- 高峰流量直接打数据库。
- 下单链路包含太多非核心操作。
- 写日志、发通知、发短信阻塞主流程。
- 突发请求导致系统瞬时压力过大。
- 消费端处理不过来导致消息堆积。
- 消息重复消费。
- 消息丢失。
- 异步后数据最终一致性问题。

### 具体优化手段

#### 异步削峰

- 请求先写 MQ。
- 消费端按能力慢慢处理。
- 控制消费者并发。
- 高峰流量排队。
- 保护数据库和下游服务。

#### 主链路缩短

- 下单后发短信异步。
- 支付成功后积分、优惠券异步。
- 操作日志异步写入。
- 通知类任务异步处理。
- 报表统计离线处理。

#### 可靠性设计

- 生产端确认机制。
- 消费端手动 ack。
- 消费失败重试。
- 死信队列。
- 本地消息表。
- 事务消息。
- 消费幂等。
- 消息状态补偿任务。

### 工具/指标

- MQ 堆积量。
- 消费延迟。
- 生产 TPS。
- 消费 TPS。
- 消费失败次数。
- 重试次数。
- 死信队列数量。
- 消费者线程数。
- 业务最终一致性校验结果。

### 串联方式

- “如果主链路包含非核心慢操作，可以异步化。”
- “如果流量有明显峰值，可以用 MQ 削峰。”
- “异步化后要补充幂等、重试和补偿机制。”
- “如果消息堆积，要看消费能力、下游瓶颈和消费者并发。”
- “MQ 是稳定性和吞吐优化，不是所有场景都适合。”

## 8. 架构层优化：当单点优化不够时扩大系统能力

### 回答要点

- 当单机、单库、单服务达到瓶颈时，需要从架构层优化。
- 架构优化包括扩容、拆分、限流、降级、读写分离、分库分表、冷热分离。
- 架构优化会引入复杂度，所以要基于瓶颈和业务增长判断是否值得。
- 高并发系统不是靠一个手段解决，而是多层保护。

### 常见问题

- 单机 CPU 或内存瓶颈。
- 单库读写压力过高。
- 单表数据量过大。
- 高峰流量超过系统承载。
- 下游故障影响核心链路。
- 历史数据拖慢在线查询。
- 报表统计影响交易库。
- 服务职责过重导致扩展困难。

### 具体优化手段

#### 水平扩容

- 增加应用实例。
- 负载均衡分摊流量。
- 服务无状态化。
- Session 外置到 Redis。
- K8s HPA 自动扩缩容。

#### 数据架构

- 读写分离。
- 分库分表。
- 冷热数据分离。
- 历史数据归档。
- Elasticsearch 支撑复杂搜索。
- OLAP 支撑报表分析。
- 预计算降低实时计算压力。

#### 流量治理

- 网关限流。
- 服务限流。
- 用户级限流。
- 热点参数限流。
- 熔断降级。
- 黑白名单。
- 流量预案。

#### 服务拆分

- 按业务边界拆分服务。
- 核心链路和非核心链路拆分。
- 读服务和写服务分离。
- 查询模型和写入模型分离。
- 避免过度拆分导致链路变长。

### 工具/指标

- 服务实例 CPU、内存。
- 单实例 QPS。
- 负载均衡分布。
- 数据库主从延迟。
- 单表数据量。
- 分片热点。
- 网关 QPS。
- 限流次数。
- 熔断降级次数。
- 容量评估报告。

### 串联方式

- “如果单机资源打满，可以先水平扩容。”
- “如果应用扩容后数据库成为瓶颈，要优化数据库或加缓存。”
- “如果单库到瓶颈，再考虑读写分离和分库分表。”
- “如果突发流量高，要限流、削峰和降级。”
- “架构优化要考虑收益和复杂度，不能过早设计。”

## 9. 验证与复盘：证明优化真的有效

### 回答要点

- 性能优化必须有前后对比。
- 不能只说“感觉快了”，要用指标证明。
- 要关注平均值，也要关注 P95/P99。
- 要确认有没有引入新问题，比如错误率升高、数据不一致、资源成本增加。
- 优化后要沉淀监控、告警和容量评估。

### 验证指标

- QPS。
- 平均响应时间。
- P95/P99 响应时间。
- 错误率。
- CPU 使用率。
- 内存使用率。
- Full GC 次数和停顿时间。
- 慢 SQL 数量。
- 数据库 CPU 和连接数。
- Redis 命中率。
- 线程池队列长度。
- MQ 堆积量。
- 下游超时次数。

### 验证方式

- 相同压测模型前后对比。
- 灰度发布观察指标。
- 线上监控观察一段时间。
- 回归测试确认功能正确。
- 核对业务数据一致性。
- 保留回滚方案。

### 串联方式

- “优化前记录基线指标。”
- “优化后用同样的流量模型压测。”
- “如果 P99 下降但错误率上升，说明优化不完整。”
- “如果接口变快但数据库压力转移到 Redis，也要评估 Redis 是否扛得住。”
- “性能优化最后要形成监控和告警，而不是一次性动作。”
