
## 14:00-16:00

#### 回溯算法实践——N皇后问题
- 前置技巧：
	- 一行只能放一个皇后，`board[i][0..j]`都不能再放，即只做「不同行」验证
	- 对角线的验证：皇后只从上往下逐层放置，下方还没放，只需检查「左上」`i--;j--`和「右上」`i--;j++`
	- String字符串与char数组的相互转换 String s=new String(charArr)<==>char`[]` charArr=s.toCharArray();
- 核心代码：
- ```java
  class Solution {
    List<List<String>> result=new ArrayList<>();
    public List<List<String>> solveNQueens(int n) {
        //初始化结果列表
        List<String> board = new ArrayList<>();
        //初始化棋盘 n*n
        for (int i = 0; i < n; i++) {
            board.add(".".repeat(n));
        }
        backTrack(0, board);

        return result;
    }

    public void backTrack(int index, List<String> board) {
        //基准条件
        if (index == board.size()){
            result.add(new ArrayList<>(board));
            return;
        }


        //节点扇出个数
        int n = board.get(index).length();
        for (int i = 0; i < n; i++) {
            if (!isValid(index, i, board)) {
                continue;
            }
            //新增路径
            char[] newRow = board.get(index).toCharArray();
            newRow[i] = 'Q';
            board.set(index, new String(newRow));

            //子节点递归
            backTrack(index + 1, board);

            //撤销路径
            newRow[i] = '.';
            board.set(index, new String(newRow));

        }
    }

    public boolean isValid(int r, int c, List<String> board) {
        int n = board.size();
        for (int i = 0; i < r; i++) {
            if (board.get(i).charAt(c) == 'Q')
                return false;
        }

        //左上角判断
        for (int i = r - 1, j = c - 1; j >= 0 && i >= 0; i--, j--) {
            if (board.get(i).charAt(j) == 'Q')
                return false;
        }

        //右上角判断
        for (int i = r - 1, j = c + 1; j < n && i >= 0; j++, i--) {
            if (board.get(i).charAt(j) == 'Q')
                return false;
        }

        return true;
    }
}
  ```

## 16:00-17:00

#### 如何进行SQL调优
- 命中索引：
	- 保证索引命中，杜绝全表扫描 or 索引全扫描
	- 保证索引最优
		- 按高区分度、高频访问等原则合理设计联合索引
		- 遵循最左前缀原则
		- 杜绝 函数操作、类型转换、模糊查询、逻辑非等索引失效的写法
- 减少回表
	- 使用索引覆盖，将查询字段、where列、分组列和排序列都放进联合索引
- 减少IO
	- 主键设计尽量短小，高扇出率保证树更矮，IO更少
	- 控制结果集，使用limit分批查询
	- 冷热数据分离，