
### 1.kafka手动提交设置

```properties
# 关闭自动提交
enable.auto.commit=false
```

```java
//消费者手动提交
consumer.commitSync(toCommit);
```


### 2.多消费者并发消费

#### 1）外层实现
```java
// 有界队列：连接消费者和写入器的桥梁  
BlockingQueue<QueueEntry> dataQueue = new LinkedBlockingQueue<>(QUEUE_CAPACITY);


// 写入线程（唯一）  
WriterWorker writerWorker = new WriterWorker(dataQueue, fileWriter);  
Thread writerThread = new Thread(writerWorker, "writer-thread");

// 消费者线程（多个）  
List<KafkaConsumerWorker> consumerWorkers = new ArrayList<>();  
List<Thread> consumerThreadList = new ArrayList<>();  
for (int i = 0; i < consumerThreads; i++) {  
    KafkaConsumerWorker worker = new KafkaConsumerWorker(props, dataQueue, i);  
    consumerWorkers.add(worker);  
    Thread t = new Thread(worker, "consumer-" + i);  
    consumerThreadList.add(t);  
}

// 启动所有线程  
writerThread.start();  
consumerThreadList.forEach(Thread::start);


// 注册 JVM 关闭钩子  
Runtime.getRuntime().addShutdownHook(new Thread(() -> {  
    log.info("Shutting down...");  
    // 1. 通知所有消费者停止 poll    consumerWorkers.forEach(KafkaConsumerWorker::shutdown);  
    // 2. 等待消费者线程退出  
    for (Thread t : consumerThreadList) {  
        try {  
            t.join(3000);  
        } catch (InterruptedException ignored) {  
        }  
    }  
    // 3. 通知 Writer 停止（此时消费者已不再往队列放数据）  
    writerWorker.shutdown();  
    // 4. 等待 Writer 把队列剩余数据写完  
    try {  
        writerThread.join(5000);  
    } catch (InterruptedException ignored) {  
    }  
    log.info("Shutdown complete");  
}, "shutdown-hook"));
```


#### 2）消费者任务
```java

```