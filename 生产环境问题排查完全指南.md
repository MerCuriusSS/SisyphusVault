
#Resources/coder 
## 1. 理论基础：什么是生产环境问题排查

### 1.1 生产环境的特点

**生产环境**（Production Environment）是指真实用户使用的系统环境，具有以下特点：

- **不可预测性**：用户行为、数据量、并发量都是不可预测的
- **高可用要求**：系统故障会直接影响业务，造成经济损失
- **调试困难**：不能随意重启、不能打断点调试、不能随意修改代码
- **数据敏感性**：涉及真实用户数据，需要严格的安全和隐私保护
- **复杂性**：多服务、多实例、多数据库、多缓存，组件众多

### 1.2 为什么需要问题排查能力

在生产环境中，问题随时可能发生：

- **功能异常**：用户操作失败、数据错误、业务逻辑错误
- **性能问题**：响应慢、超时、卡顿
- **系统故障**：服务宕机、数据库连接失败、内存溢出
- **安全问题**：攻击、数据泄露、权限绕过

**快速定位和解决问题**是保障系统稳定运行的关键能力。

### 1.3 生产排查的核心思路

```
发现问题 → 应急止损 → 定位问题 → 分析原因 → 解决问题 → 验证修复 → 总结预防
```

**关键点**：
- **应急能力**：先给故障定性，根据影响范围执行相关的止损操作（降级 / 回滚 / 限流 / 记录问题信息并实施排查）
- **可观测性**（Observability）：系统必须能够被观测，才能被排查
- **日志完整性**：关键操作必须有日志记录
- **监控覆盖**：系统状态必须被实时监控
- **追踪能力**：请求链路必须可追踪

---

## 2. 核心要素：可观测性三大支柱

现代系统的可观测性（Observability）由三大支柱构成：

### 2.1 日志（Logs）

**定义**：系统运行时产生的文本记录，记录了"发生了什么"。

**特点**：
- 离散的事件记录
- 包含详细的上下文信息
- 适合事后分析和问题定位

**典型内容**：
- 用户操作日志（谁、什么时间、做了什么、结果如何）
- 系统错误日志（异常堆栈、错误信息）
- 业务日志（订单创建、支付成功等）

### 2.2 指标（Metrics）

**定义**：系统运行时的数值型数据，记录了"系统状态如何"。

**特点**：
- 连续的时间序列数据
- 占用空间小，适合长期存储
- 适合实时监控和告警

**典型指标**：
- 系统指标：CPU、内存、磁盘、网络
- 应用指标：QPS、响应时间、错误率
- 业务指标：订单量、支付成功率、活跃用户数

### 2.3 追踪（Traces）

**定义**：记录一个请求在分布式系统中的完整调用链路。

**特点**：
- 跨服务的调用关系
- 每个环节的耗时
- 适合性能分析和依赖关系梳理

**典型场景**：
- 微服务架构中的请求追踪
- 性能瓶颈定位
- 服务依赖关系分析

---

## 3. 日志体系：问题排查的第一道防线

### 3.1 日志的重要性

日志是问题排查最重要的信息来源：
- **80%的问题可以通过日志定位**
- 日志是事后分析的唯一依据
- 日志可以还原问题发生时的完整上下文

### 3.2 日志分级策略

标准的日志级别（从低到高）：

| 级别 | 用途 | 生产环境 | 示例 |
|------|------|----------|------|
| **TRACE** | 最详细的调试信息 | ❌ 关闭 | 方法入参、出参 |
| **DEBUG** | 调试信息 | ❌ 关闭 | 中间变量、分支判断 |
| **INFO** | 重要的业务流程 | ✅ 开启 | 用户登录、订单创建 |
| **WARN** | 警告信息，不影响运行 | ✅ 开启 | 配置缺失、降级处理 |
| **ERROR** | 错误信息，需要关注 | ✅ 开启 | 异常、失败操作 |

**原则**：
- 生产环境通常使用 **INFO** 级别
- ERROR 日志必须有人处理
- 避免日志过多（影响性能）或过少（无法排查）

### 3.3 RuoYi-Vue-Plus 的日志实现

#### 3.3.1 Logback 配置

项目使用 **Logback** 作为日志框架，配置文件：`ruoyi-admin/src/main/resources/logback-plus.xml`

**核心配置**：

```xml
<!-- 日志存储路径 -->
<property name="log.path" value="./logs"/>

<!-- 控制台日志格式（带颜色） -->
<property name="console.log.pattern"
          value="%cyan(%d{yyyy-MM-dd HH:mm:ss}) %green([%thread]) %highlight(%-5level) %boldMagenta(%logger{36}%n) - %msg%n"/>

<!-- 文件日志格式（纯文本） -->
<property name="log.pattern" value="%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"/>
```

**日志文件结构**：

```
./logs/
├── sys-console.log          # 实时控制台日志（保留1天）
├── sys-info.log             # INFO级别日志（当天）
├── sys-info.2024-02-24.log.gz  # INFO历史日志（压缩，保留60天）
├── sys-error.log            # ERROR级别日志（当天）
└── sys-error.2024-02-24.log.gz # ERROR历史日志（压缩，保留60天）
```


#### 3.3.2 日志文件配置详解

**1. 控制台日志文件**（sys-console.log）

```xml
<appender name="file_console" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${log.path}/sys-console.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <!-- 按日期滚动 -->
        <fileNamePattern>${log.path}/sys-console.%d{yyyy-MM-dd}.log</fileNamePattern>
        <!-- 只保留1天 -->
        <maxHistory>1</maxHistory>
    </rollingPolicy>
    <encoder>
        <pattern>${log.pattern}</pattern>
        <charset>utf-8</charset>
    </encoder>
    <!-- 只记录INFO及以上级别 -->
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
        <level>INFO</level>
    </filter>
</appender>
```

**用途**：实时查看最近的日志，通过 Spring Boot Admin 在线查看。

**2. INFO 级别日志**（sys-info.log）

```xml
<appender name="file_info" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${log.path}/sys-info.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <!-- 按日期滚动，自动压缩 -->
        <fileNamePattern>${log.path}/sys-info.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
        <!-- 保留60天 -->
        <maxHistory>60</maxHistory>
    </rollingPolicy>
    <encoder>
        <pattern>${log.pattern}</pattern>
    </encoder>
    <!-- 只记录INFO级别（精确匹配） -->
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
        <level>INFO</level>
        <onMatch>ACCEPT</onMatch>
        <onMismatch>DENY</onMismatch>
    </filter>
</appender>
```

**用途**：记录正常的业务流程，用于业务分析和问题追溯。

**3. ERROR 级别日志**（sys-error.log）

```xml
<appender name="file_error" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${log.path}/sys-error.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
        <fileNamePattern>${log.path}/sys-error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
        <maxHistory>60</maxHistory>
    </rollingPolicy>
    <encoder>
        <pattern>${log.pattern}</pattern>
    </encoder>
    <!-- 只记录ERROR级别 -->
    <filter class="ch.qos.logback.classic.filter.LevelFilter">
        <level>ERROR</level>
        <onMatch>ACCEPT</onMatch>
        <onMismatch>DENY</onMismatch>
    </filter>
</appender>
```

**用途**：记录所有错误和异常，是问题排查的重点关注对象。

#### 3.3.3 异步日志优化

为了避免日志写入阻塞业务线程，项目使用**异步日志**：

```xml
<!-- INFO异步输出 -->
<appender name="async_info" class="ch.qos.logback.classic.AsyncAppender">
    <!-- 不丢失日志：队列满时不丢弃任何级别的日志 -->
    <discardingThreshold>0</discardingThreshold>
    <!-- 队列大小：512（默认256） -->
    <queueSize>512</queueSize>
    <!-- 关联同步appender -->
    <appender-ref ref="file_info"/>
</appender>

<!-- ERROR异步输出 -->
<appender name="async_error" class="ch.qos.logback.classic.AsyncAppender">
    <discardingThreshold>0</discardingThreshold>
    <queueSize>512</queueSize>
    <appender-ref ref="file_error"/>
</appender>
```

**工作原理**：
1. 业务线程将日志放入内存队列（非阻塞）
2. 后台线程从队列取出日志并写入文件
3. 业务线程不等待磁盘IO，性能提升明显

**注意事项**：
- `discardingThreshold=0`：确保不丢失日志（默认队列80%满时会丢弃低级别日志）
- `queueSize=512`：队列大小需要根据日志量调整

#### 3.3.4 生产环境日志配置

在 `application-prod.yml` 中配置日志级别：

```yaml
logging:
  level:
    org.dromara: info          # 项目代码使用INFO级别
    org.springframework: warn  # Spring框架使用WARN级别
  config: classpath:logback-plus.xml
```

**在线查看日志**：

```yaml
management:
  endpoint:
    logfile:
      external-file: ./logs/sys-console.log  # 指定日志文件路径
```

通过 Spring Boot Admin 访问 `/actuator/logfile` 可以在线查看和搜索日志。

### 3.4 业务日志记录

#### 3.4.1 操作日志（@Log 注解）

项目通过 AOP 切面自动记录用户操作日志。

**使用方式**：

```java
@RestController
@RequestMapping("/system/user")
public class SysUserController {

    @SaCheckPermission("system:user:add")
    @Log(title = "用户管理", businessType = BusinessType.INSERT)
    @PostMapping
    public R<Void> add(@Validated @RequestBody SysUserBo user) {
        return toAjax(userService.insertUser(user));
    }
}
```

**核心实现**：`LogAspect.java`

```java
@Slf4j
@Aspect
@AutoConfiguration
public class LogAspect {

    private static final ThreadLocal<StopWatch> KEY_CACHE = new ThreadLocal<>();

    /**
     * 处理请求前执行：开始计时
     */
    @Before(value = "@annotation(controllerLog)")
    public void doBefore(JoinPoint joinPoint, Log controllerLog) {
        StopWatch stopWatch = new StopWatch();
        KEY_CACHE.set(stopWatch);
        stopWatch.start();
    }

    /**
     * 处理完请求后执行：记录日志
     */
    @AfterReturning(pointcut = "@annotation(controllerLog)", returning = "jsonResult")
    public void doAfterReturning(JoinPoint joinPoint, Log controllerLog, Object jsonResult) {
        handleLog(joinPoint, controllerLog, null, jsonResult);
    }

    /**
     * 拦截异常操作：记录错误日志
     */
    @AfterThrowing(value = "@annotation(controllerLog)", throwing = "e")
    public void doAfterThrowing(JoinPoint joinPoint, Log controllerLog, Exception e) {
        handleLog(joinPoint, controllerLog, e, null);
    }

    protected void handleLog(final JoinPoint joinPoint, Log controllerLog, 
                            final Exception e, Object jsonResult) {
        try {
            // 创建操作日志事件
            OperLogEvent operLog = new OperLogEvent();
            operLog.setTenantId(LoginHelper.getTenantId());
            operLog.setStatus(BusinessStatus.SUCCESS.ordinal());
            
            // 记录请求信息
            String ip = ServletUtils.getClientIP();
            operLog.setOperIp(ip);
            operLog.setOperUrl(ServletUtils.getRequest().getRequestURI());
            
            // 记录操作人信息
            LoginUser loginUser = LoginHelper.getLoginUser();
            operLog.setOperName(loginUser.getUsername());
            operLog.setDeptName(loginUser.getDeptName());

            // 如果有异常，记录异常信息
            if (e != null) {
                operLog.setStatus(BusinessStatus.FAIL.ordinal());
                operLog.setErrorMsg(StringUtils.substring(e.getMessage(), 0, 3800));
            }
            
            // 记录方法名
            String className = joinPoint.getTarget().getClass().getName();
            String methodName = joinPoint.getSignature().getName();
            operLog.setMethod(className + "." + methodName + "()");
            
            // 记录请求方式
            operLog.setRequestMethod(ServletUtils.getRequest().getMethod());
            
            // 处理注解参数
            getControllerMethodDescription(joinPoint, controllerLog, operLog, jsonResult);
            
            // 记录耗时
            StopWatch stopWatch = KEY_CACHE.get();
            stopWatch.stop();
            operLog.setCostTime(stopWatch.getDuration().toMillis());
            
            // 发布事件，异步保存到数据库
            SpringUtils.context().publishEvent(operLog);
        } catch (Exception exp) {
            log.error("异常信息:{}", exp.getMessage());
        } finally {
            KEY_CACHE.remove();
        }
    }
}
```

**记录内容**：
- 操作人：用户名、部门
- 操作信息：IP地址、请求URL、请求方式
- 业务信息：操作类型（新增/修改/删除等）、业务标题
- 请求参数：自动过滤敏感字段（password等）
- 响应结果：可选记录
- 耗时：毫秒级
- 异常信息：如果操作失败

**敏感字段过滤**：

```java
private void setRequestValue(JoinPoint joinPoint, OperLogEvent operLog, String[] excludeParamNames) {
    // 获取请求参数
    Map<String, String> paramsMap = ServletUtils.getParamMap(ServletUtils.getRequest());
    
    // 移除敏感字段
    MapUtil.removeAny(paramsMap, SystemConstants.EXCLUDE_PROPERTIES);  // password, oldPassword, newPassword等
    MapUtil.removeAny(paramsMap, excludeParamNames);  // 自定义排除字段
    
    operLog.setOperParam(StringUtils.substring(JsonUtils.toJsonString(paramsMap), 0, 3800));
}
```

**异步事件处理**：

操作日志通过 Spring 事件机制异步保存，不阻塞业务线程：

```java
// 发布事件
SpringUtils.context().publishEvent(operLog);

// 事件监听器（SysOperLogServiceImpl）
@EventListener
public void recordOper(OperLogEvent operLogEvent) {
    // 异步保存到数据库 sys_oper_log 表
    operLogMapper.insert(operLogEvent);
}
```

**数据库存储**：

操作日志保存在 `sys_oper_log` 表中，可通过管理界面查询：

```
系统监控 → 操作日志
```

可以按用户、时间、操作类型、状态等条件查询。


## 5. 异常处理：统一捕获与记录

### 5.1 全局异常处理器

项目通过 `@RestControllerAdvice` 统一捕获和处理所有异常。

**核心实现**：`GlobalExceptionHandler.java`

```java
@Slf4j
@RestControllerAdvice
public class GlobalExceptionHandler {

    /**
     * 业务异常
     */
    @ExceptionHandler(ServiceException.class)
    public R<Void> handleServiceException(ServiceException e, HttpServletRequest request) {
        log.error(e.getMessage());
        Integer code = e.getCode();
        return ObjectUtil.isNotNull(code) ? R.fail(code, e.getMessage()) : R.fail(e.getMessage());
    }

    /**
     * 请求方式不支持
     */
    @ExceptionHandler(HttpRequestMethodNotSupportedException.class)
    public R<Void> handleHttpRequestMethodNotSupported(HttpRequestMethodNotSupportedException e,
                                                       HttpServletRequest request) {
        String requestURI = request.getRequestURI();
        log.error("请求地址'{}',不支持'{}'请求", requestURI, e.getMethod());
        return R.fail(HttpStatus.HTTP_BAD_METHOD, e.getMessage());
    }

    /**
     * 拦截未知的运行时异常
     */
    @ExceptionHandler(RuntimeException.class)
    public R<Void> handleRuntimeException(RuntimeException e, HttpServletRequest request) {
        String requestURI = request.getRequestURI();
        log.error("请求地址'{}',发生未知异常.", requestURI, e);
        return R.fail(e.getMessage());
    }

    /**
     * 系统异常
     */
    @ExceptionHandler(Exception.class)
    public R<Void> handleException(Exception e, HttpServletRequest request) {
        String requestURI = request.getRequestURI();
        log.error("请求地址'{}',发生系统异常.", requestURI, e);
        return R.fail(e.getMessage());
    }
}
```

**关键点**：
- 所有异常都记录请求URI，方便定位问题
- 异常信息记录到 `sys-error.log`
- 返回统一的错误响应格式

### 5.2 专用异常处理器

**认证异常**：`SaTokenExceptionHandler`
- 未登录异常
- 权限不足异常
- Token过期异常

**数据库异常**：`MybatisExceptionHandler`
- 重复键异常（DuplicateKeyException）
- 数据源未找到异常
- MyBatis系统异常

**分布式锁异常**：`RedisExceptionHandler`
- Lock4j获取锁失败异常

### 5.3 异常排查流程

当用户反馈"操作失败"时：

1. **查看操作日志**
   - 管理界面 → 系统监控 → 操作日志
   - 找到该用户的操作记录
   - 查看状态是否为"失败"
   - 查看错误信息

2. **查看ERROR日志**
   - 打开 `./logs/sys-error.log`
   - 搜索操作时间点附近的异常
   - 查看完整的异常堆栈

3. **定位问题代码**
   - 根据异常堆栈找到出错的类和方法
   - 查看该方法的业务逻辑
   - 分析异常原因

---

## 6. 性能监控：发现瓶颈与优化

### 6.1 请求耗时监控

通过 `PlusWebInvokeTimeInterceptor` 自动记录每个请求的耗时。

**日志示例**：

```
2024-02-25 10:30:15 [http-nio-8080-exec-1] INFO  - [PLUS]开始请求 => URL[POST /system/user],参数类型[json],参数:[{"userName":"test"}]
2024-02-25 10:30:15 [http-nio-8080-exec-1] INFO  - [PLUS]结束请求 => URL[POST /system/user],耗时:[123]毫秒
```

**排查慢请求**：

```bash
# 搜索耗时超过1000毫秒的请求
grep "耗时:\[1[0-9][0-9][0-9]\]" ./logs/sys-console.log
grep "耗时:\[[2-9][0-9][0-9][0-9]\]" ./logs/sys-console.log
```

### 6.2 数据库性能监控

**开发环境**：使用 P6spy 监控SQL

```yaml
spring.datasource.dynamic:
  p6spy: true  # 开发环境开启
```

**生产环境**：关闭P6spy，使用数据库慢查询日志

```sql
-- MySQL慢查询配置
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;  -- 超过1秒的查询记录
SET GLOBAL slow_query_log_file = '/var/log/mysql/slow.log';
```

**HikariCP连接池监控**：
### 方式1：Actuator
#### 1.1 配置Actuator实现「采集」并「访问」hikaricp指标数据的任务。
```yml
management:
  # 1. 端点相关配置（Actuator的核心入口）
  endpoints:
    # 1.1 仅针对Web环境的端点配置（比如HTTP访问的端点）
    web:
      # 1.2 控制哪些端点通过HTTP暴露（白名单）
      exposure:
        include: metrics,health  # 只允许通过HTTP访问/actuator/metrics和/actuator/health
  # 2. 指标采集相关配置（和端点暴露无关，是指标本身的规则）
  metrics:
    # 2.2 开启特定组件的指标采集（这里是HikariCP）
    enable:
      hikaricp: true  # 让系统采集HikariCP连接池的指标数据
```

#### 1.2.通过访问路径查看指标数据
```bash
# 查看活跃连接数
curl http://localhost:8080/actuator/metrics/hikaricp.connections.active

# 查看等待连接的线程数（如果>0说明连接池不够）
curl http://localhost:8080/actuator/metrics/hikaricp.connections.pending
```

### 方式2：集成 Prometheus+Grafana

**目的：Prometheus充当仓库，定时拉取Actuator并存储数据；Grafana提供可视化，展示历史趋势**

#### application-prod.yml
```yaml
management:
  metrics:
    export:
      prometheus:
        enabled: true  # 开启Prometheus导出
  endpoints:
    web:
      exposure:
        include: prometheus,metrics,health  # 暴露prometheus端点
```

#### prometheus.yml
```yaml
scrape_configs:
  - job_name: 'ruoyi-vue-plus'
    scrape_interval: 5s  # 每5秒采集一次
    static_configs:
      - targets: ['localhost:8080']  # 应用地址
    metrics_path: '/actuator/prometheus'
```

#### Grafana 导入 HikariCP 监控面板

1. 打开 Grafana → 导入面板 → 输入 ID：`11921`（官方 HikariCP 面板）；
2. 配置数据源为 Prometheus；
3. 即可看到连接池的实时监控图表（活跃连接、空闲连接、等待线程、连接创建 / 关闭速率等）。


### 6.3 JVM性能监控

**通过Actuator查看**：




```bash
# JVM内存使用
curl http://localhost:8080/actuator/metrics/jvm.memory.used

# GC统计
curl http://localhost:8080/actuator/metrics/jvm.gc.pause

# 线程数
curl http://localhost:8080/actuator/metrics/jvm.threads.live
```


**通过Spring Boot Admin查看**：
- 访问 http://localhost:9090/admin
- 选择应用实例
- 查看 "Details" → "Metrics"

**使用JVM工具**——用 JDK 自带工具（`jstat`/`jmap`/`jstack`/`jcmd`）抓取 JVM 运行状态


#### 1. 先找到 Java 进程 ID（PID）
```bash
# 方式1：jps（JDK自带，最简洁）
jps -l  # 显示PID和应用主类/包名
# 示例输出：12345 org.springframework.boot.loader.JarLauncher

# 方式2：ps（通用）
ps aux | grep java | grep -v grep  # 过滤出Java进程
```



```bash
# jstat - 查看GC统计
jstat -gc <pid> 1000 10

# jmap - 生成堆内存快照
jmap -dump:format=b,file=heapdump.hprof <pid>

# jstack - 生成线程快照
jstack <pid> > thread.dump
```

### 6.4 Redis性能监控

```bash
# 连接Redis
redis-cli -h localhost -p 6379 -a ruoyi123

# 查看慢查询（超过10ms的命令）
SLOWLOG GET 10

# 查看内存使用
INFO memory

# 查看连接数
INFO clients

# 实时监控命令
MONITOR
```

---

## 7. 分布式追踪：跨服务调用链路

### 7.1 SkyWalking 集成（可选）

项目预留了 SkyWalking 分布式追踪的集成配置。

**启用步骤**：

1. **取消依赖注释**（`ruoyi-admin/pom.xml`）：

```xml
<!-- skywalking 整合 logback -->
<dependency>
    <groupId>org.apache.skywalking</groupId>
    <artifactId>apm-toolkit-logback-1.x</artifactId>
</dependency>
<dependency>
    <groupId>org.apache.skywalking</groupId>
    <artifactId>apm-toolkit-trace</artifactId>
</dependency>
```

2. **取消Logback配置注释**（`logback-plus.xml`）：

```xml
<!-- 整合 skywalking 控制台输出 tid -->
<appender name="console" class="ch.qos.logback.core.ConsoleAppender">
    <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
        <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
            <pattern>[%tid] ${console.log.pattern}</pattern>
        </layout>
        <charset>utf-8</charset>
    </encoder>
</appender>

<!-- 整合 skywalking 推送采集日志 -->
<appender name="sky_log" class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.log.GRPCLogClientAppender">
    <encoder class="ch.qos.logback.core.encoder.LayoutWrappingEncoder">
        <layout class="org.apache.skywalking.apm.toolkit.log.logback.v1.x.TraceIdPatternLogbackLayout">
            <pattern>[%tid] ${console.log.pattern}</pattern>
        </layout>
        <charset>utf-8</charset>
    </encoder>
</appender>
```

3. **启动时添加Agent**：

```bash
java -javaagent:/path/to/skywalking-agent.jar \
     -Dskywalking.agent.service_name=ruoyi-vue-plus \
     -Dskywalking.collector.backend_service=127.0.0.1:11800 \
     -jar ruoyi-admin.jar
```

**功能**：
- 自动追踪HTTP请求、数据库查询、Redis操作
- 日志自动关联TraceId
- 可视化调用链路和性能分析

### 7.2 TraceId 的作用

在分布式系统中，一个用户请求可能经过多个服务：

```
用户请求 → 网关 → 用户服务 → 订单服务 → 支付服务
```

TraceId 是一个唯一标识，贯穿整个调用链路：

```
[TID:abc123] 用户服务收到请求
[TID:abc123] 用户服务查询数据库
[TID:abc123] 用户服务调用订单服务
[TID:abc123] 订单服务收到请求
[TID:abc123] 订单服务调用支付服务
[TID:abc123] 支付服务收到请求
```

通过TraceId可以快速找到一个请求的所有相关日志。

---

## 8. 实战案例：常见问题排查流程

### 8.1 案例1：用户反馈"登录失败"

**问题描述**：用户反馈无法登录系统。

**排查步骤**：

1. **查看登录日志**
   ```
   管理界面 → 系统监控 → 登录日志
   搜索该用户的登录记录
   ```

   可能的结果：
   - 密码错误（提示用户重置密码）
   - 账号锁定（检查错误次数，解锁账号）
   - 账号禁��（��查用户状态）

2. **查看ERROR日志**
   ```bash
   tail -f ./logs/sys-error.log
   ```

   可能的异常：
   - 数据库连接失败
   - Redis连接失败
   - Token生成失败

3. **检查系统状态**
   ```bash
   curl http://localhost:8080/actuator/health
   ```

   检查数据库、Redis是否正常。

### 8.2 案例2：接口响应慢

**问题描述**：某个接口响应时间超过5秒。

**排查步骤**：

1. **查看请求耗时日志**
   ```bash
   grep "POST /system/user/list" ./logs/sys-console.log | grep "耗时"
   ```

   确认耗时确实很长。

2. **分析慢在哪里**

   可能的原因：
   - **数据库慢查询**：查看慢查询日志
   - **数据量大**：检查是否缺少分页
   - **N+1查询**：检查是否有循环查询数据库
   - **外部调用慢**：检查是否调用了慢的外部接口
   - **锁等待**：检查是否有分布式锁竞争

3. **数据库慢查询分析**
   ```sql
   -- 查看MySQL慢查询
   SELECT * FROM mysql.slow_log ORDER BY start_time DESC LIMIT 10;
   ```

   或者查看慢查询日志文件：
   ```bash
   tail -f /var/log/mysql/slow.log
   ```

4. **优化方案**
   - 添加数据库索引
   - 优化SQL查询（避免SELECT *）
   - 添加缓存
   - 异步处理

### 8.3 案例3：服务突然不可用

**问题描述**：服务突然无法访问。

**排查步骤**：

1. **检查服务是否运行**
   ```bash
   # Linux
   ps aux | grep java

   # Windows
   tasklist | findstr java
   ```

2. **查看Spring Boot Admin**
   ```
   http://localhost:9090/admin
   ```

   查看服务状态是否为DOWN。

3. **查看启动日志**
   ```bash
   tail -100 ./logs/sys-console.log
   ```

   可能的原因：
   - 端口被占用
   - 配置错误（数据库连接失败）
   - 内存溢出（OOM）
   - 磁盘空间不足

4. **查看系统日志**
   ```bash
   # Linux - 查看是否被OOM Killer杀掉
   dmesg | grep -i "out of memory"

   # 查看磁盘空间
   df -h
   ```

5. **解决方案**
   - 修复配置错误
   - 增加JVM内存
   - 清理磁盘空间
   - 重启服务

### 8.4 案例4：内存泄漏

**问题描述**：服务运行一段时间后内存持续增长，最终OOM。

**排查步骤**：

1. **监控内存趋势**
   ```bash
   # 通过Actuator查看内存使用
   curl http://localhost:8080/actuator/metrics/jvm.memory.used
   ```

   或通过Spring Boot Admin查看内存趋势图。

2. **生成堆内存快照**
   ```bash
   # 方式1：通过jmap
   jmap -dump:format=b,file=heapdump.hprof <pid>

   # 方式2：通过Actuator
   curl http://localhost:8080/actuator/heapdump -o heapdump.hprof
   ```

3. **分析堆内存快照**

   使用工具：
   - Eclipse MAT（Memory Analyzer Tool）
   - VisualVM
   - JProfiler

   查找：
   - 占用内存最多的对象
   - 对象的引用链
   - 是否有大量重复对象

4. **常见内存泄漏原因**
   - ThreadLocal未清理
   - 集合类持续增长（缓存未设置过期）
   - 数据库连接未关闭
   - 监听器未注销

### 8.5 案例5：线程死锁

**问题描述**：服务卡住不响应，CPU使用率低。

**排查步骤**：

1. **生成线程快照**
   ```bash
   # 方式1：通过jstack
   jstack <pid> > thread.dump

   # 方式2：通过Actuator
   curl http://localhost:8080/actuator/threaddump > thread.dump
   ```

2. **分析线程快照**

   查找：
   - `BLOCKED` 状态的线程
   - `WAITING` 状态的线程
   - 死锁信息（jstack会自动检测）

3. **死锁示例**
   ```
   Found one Java-level deadlock:
   =============================
   "Thread-1":
     waiting to lock monitor 0x00007f8a1c004e00 (object 0x00000000d5f78a20, a java.lang.Object),
     which is held by "Thread-2"
   "Thread-2":
     waiting to lock monitor 0x00007f8a1c007350 (object 0x00000000d5f78a30, a java.lang.Object),
     which is held by "Thread-1"
   ```

4. **解决方案**
   - 调整锁的顺序
   - 使用超时锁
   - 重构代码避免嵌套锁

---

## 9. 最佳实践与建议

### 9.1 日志最佳实践

1. **合理使用日志级别**
   - ERROR：必须处理的错误
   - WARN：需要关注但不影响运行
   - INFO：重要的业务流程
   - DEBUG：调试信息（生产环境关闭）

2. **日志内容要完整**
   ```java
   // ❌ 不好的日志
   log.error("操作失败");

   // ✅ 好的日志
   log.error("用户[{}]创建订单失败，订单号[{}]，原因：{}", userId, orderId, e.getMessage(), e);
   ```

3. **避免日志过多**
   - 不要在循环中打印日志
   - 不要打印大对象（如整个请求体）
   - 使用日志级别控制

4. **敏感信息脱敏**
   - 密码、身份证号、手机号等
   - 项目已自动过滤password等字段

### 9.2 监控最佳实践

1. **设置告警阈值**
   - CPU使用率 > 80%
   - 内存使用率 > 85%
   - 磁盘使用率 > 90%
   - 接口响应时间 > 3秒
   - 错误率 > 1%

2. **定期检查**
   - 每天查看ERROR日志
   - 每周查看慢查询日志
   - 每月查看性能趋势

3. **建立监控大盘**
   - 使用Grafana + Prometheus
   - 或使用云服务商的监控服务

### 9.3 异常处理最佳实践

1. **不要吞掉异常**
   ```java
   // ❌ 不好的做法
   try {
       // ...
   } catch (Exception e) {
       // 什么都不做
   }

   // ✅ 好的做法
   try {
       // ...
   } catch (Exception e) {
       log.error("操作失败", e);
       throw new ServiceException("操作失败：" + e.getMessage());
   }
   ```

2. **异常信息要明确**
   ```java
   // ❌ 不好的异常信息
   throw new ServiceException("操作失败");

   // ✅ 好的异常信息
   throw new ServiceException("用户[" + userId + "]不存在，无法执行操作");
   ```

3. **区分业务异常和系统异常**
   - 业务异常：用户输入错误、权限不足等（不需要记录堆栈）
   - 系统异常：数据库连接失败、空指针等（需要记录堆栈）

### 9.4 性能优化建议

1. **数据库优化**
   - 添加合适的索引
   - 避免SELECT *
   - 使用分页查询
   - 避免N+1查询

2. **缓存使用**
   - 热点数据使用Redis缓存
   - 设置合理的过期时间
   - 注意缓存穿透、击穿、雪崩

3. **异步处理**
   - 耗时操作使用异步
   - 使用消息队列解耦

4. **连接池配置**
   - 根据并发量调整连接池大小
   - 监控连接池使用情况

### 9.5 生产环境检查清单

**部署前**：
- [ ] 日志级别设置为INFO
- [ ] 关闭P6spy SQL监控
- [ ] 配置Spring Boot Admin
- [ ] 配置Actuator端点
- [ ] 设置合理的JVM参数
- [ ] 配置数据库连接池
- [ ] 配置Redis连接池
- [ ] 设置日志文件滚动策略

**运行中**：
- [ ] 每天查看ERROR日志
- [ ] 监控服务健康状态
- [ ] 监控JVM内存和GC
- [ ] 监控数据库连接池
- [ ] 监控接口响应时间
- [ ] 定期备份数据

**问题发生时**：
- [ ] 查看操作日志
- [ ] 查看ERROR日志
- [ ] 查看系统监控指标
- [ ] 生成线程快照
- [ ] 生成堆内存快照
- [ ] 查看数据库慢查询

---

## 10. 总结

生产环境问题排查是一个系统工程，需要：

1. **完善的日志体系**：记录关键操作和异常
2. **实时的监控体系**：掌握系统健康状态
3. **统一的异常处理**：捕获和记录所有异常
4. **性能监控工具**：发现瓶颈和优化点
5. **分布式追踪**：跨服务调用链路分析

RuoYi-Vue-Plus 项目已经实现了完善的监控体系：
- Logback 日志框架（分级、异步、滚动）
- 操作日志和登录日志（数据库存储）
- Spring Boot Actuator（监控端点）
- Spring Boot Admin（可视化监控）
- 全局异常处理（统一捕获）
- 请求性能监控（耗时记录）
- SkyWalking 集成（可选）

掌握这些工具和方法，就能快速定位和解决生产环境的各种问题。

---

**相关文件**：
- 日志配置：`ruoyi-admin/src/main/resources/logback-plus.xml`
- 生产配置：`ruoyi-admin/src/main/resources/application-prod.yml`
- 操作日志：`ruoyi-common/ruoyi-common-log/src/main/java/org/dromara/common/log/aspect/LogAspect.java`
- 异常处理：`ruoyi-common/ruoyi-common-web/src/main/java/org/dromara/common/web/handler/GlobalExceptionHandler.java`
- 性能监控：`ruoyi-common/ruoyi-common-web/src/main/java/org/dromara/common/web/interceptor/PlusWebInvokeTimeInterceptor.java`
- 监控中心：`ruoyi-extend/ruoyi-monitor-admin/`
