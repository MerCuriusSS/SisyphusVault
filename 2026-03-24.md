11:37-12:37
**kafka消费组总结（概念/作用）**
- 消费组就是为一批消费者设定一个共同的组名，用于统一管理消费进度；
- 作用：
    
    - 多消费组之间：**独立消费同一份数据**，实现类似广播的效果（每份消息每个组都能消费一遍）；
    - 单消费组内：多个消费者共同分摊消费，实现高并发；
    
- 组内规则：**一个分区只能被一个消费者消费**，以此避免重复消费；**分区数量 = 该组最大消费并发数**。

**kafka重新认识**（浅层）

14:35~15:35
kafka 重认识（深层）：
概念：大、快、稳、分布式的消息中间件（缓冲管道）
- 作用：
    - 解耦：上游不受下游服务约束，甚至可以忽略下游服务存在，只对中间件负责。
    - 削峰：承受激增流量，平缓落地给下游服务。
    - 异步：发送消息即返回，无需等待后续服务响应，用户体验上升
    - **负载均衡**：通过分区实现并行，分摊流量
        
        - 生产负载均衡：数据分摊到不同的分区中
            
            - 轮询
            - hash(key)
            - 自定义
            
        - 消费负载均衡：分区分摊到组内消费者（**一个消费组内，一个分区只被一个消费者消费**）
            
            - 范围分：range
            - 轮询分
            - 粘性分配
            
        
    - **重平衡 Rebalance**
        
        - 定义：消费者 / 分区变化时重新分配分区
        - 时机：
            
            - 消费者加入 / 离开 / 宕机
            - 分区数量增加
            - 消费者心跳超时
            
        - 重点：重平衡期间所有消费者停止消费
        
    
- 组成部分：
    - broker：消息服务器，持久化消息
    - producer：生产者
    - consumer：消费者
    - topic：消息分类（按业务分）
    - partition：数据分布式存储，决定消费并行能力
    - consumer-group：消费组；不同组独立重复消费，同组分摊消费；组内消费者数决定并发量（≤partition）
    - **offset**：
        - 定义：消费组在分区内的消费进度，唯一标识为（消费组 + 分区）
        - 作用：保证消息不丢失、不重复、可回溯。
        - 提交：推进消息消费进度
            - 自动提交：不论业务结果，定时提交；业务失败但 offset 已提交，会丢消息；简单不安全
            - 手动提交：业务处理成功后提交；保证不丢消息；
