## 9:00-10:00
- 自测 线程池 和 chm1.8的扩容过程中 sizeCtl变化
- 算法-层次遍历代码模板
- ```java
  //最简单，不含层级
  public void levelOrderTraverse1(TreeNode root){
	  if(root==null){
		  return ;
	  }
	  //核心：队列保存
	  Queue<TreeNode> q=new LinkedList<>();
	  q.offer(root);
	  while(!q.isEmpty()){
		  TreeNode cur=q.poll();
		  print(cur.val);
		  if(cur.left!=null){
			  q.offer(cur.left);
		  }
		  if(cur.right!=null){
			  q.offer(cur.right);
		  }
	  }
  }
  
  //包含层级
  public void levelOrderTraverse2(TreeNode root){
	  if(root==null){
		  return ;
	  }
	  Queue<TreeNode> q=new LinkedList<>();
	  q.offer(root);
	  while(!q.isEmpty()){
		  int sz=q.size();
		  
		  for(int i=0;i<sz;i++){
			  
		  }
	  }
  }
  ```