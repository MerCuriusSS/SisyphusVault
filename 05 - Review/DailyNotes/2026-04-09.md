## 9:00-10:00
- 自测 线程池 和 chm1.8的扩容过程中 sizeCtl变化
- 算法-层次遍历代码模板
- ```java
  //最简单，不含层级
  public void levelOrderTraverse1(TreeNode root){
	  if(root==null){
		  return ;
	  }
	  //核心：队列保存
	  Queue<TreeNode> q=new LinkedList<>();
	  q.offer(root);
	  while(!q.isEmpty()){
		  TreeNode cur=q.poll();
		  print(cur.val);
		  if(cur.left!=null){
			  q.offer(cur.left);
		  }
		  if(cur.right!=null){
			  q.offer(cur.right);
		  }
	  }
  }
  
  //包含层级
  public void levelOrderTraverse2(TreeNode root){
	  if(root==null){
		  return ;
	  }
	  Queue<TreeNode> q=new LinkedList<>();
	  q.offer(root);
	  int level=1;
	  while(!q.isEmpty()){
		  int sz=q.size();
		  T
		  //当层元素遍历
		  for(int i=0;i<sz;i++){
			  reeNode cur=q.poll();
			  print(cur.val);
			  if(cur.left!=null){
				  q.offer(cur.left);
			  }
			  
			  if(cur.right!=null){
				  q.offer(cur.right);
			  }
		  }
		  level++;
	  }
  }
  ```

## 10:00-12:30
JVM知识框架梳理
- 定义：java运行环境容器
- 核心价值：
	- 一次编译，到处运行（将字节码翻译成机器码）
	- 自动内存管理（自动分配内存、自动标记垃圾对象、自动垃圾回收）
	- JIT即时编译（兼顾开发和运行效率）
- 组成部分：
	- 类加载子系统
		- 定义：将字节码文件转化为内存中 Class 对象的核心子系统
		- 
	- 运行时数据区（JVM内存结构）
	- 执行引擎
	- 本地方法接口（JNI）
	- 垃圾回收器