---
tags:
  - Areas/Coder/工程化思维
category: 思维
status: 加工
project:
application:
source:
---
[「记忆规律」与「知识体系建设」](../../03%20-%20Areas/「记忆规律」与「知识体系建设」.md)

```
L1：核心问题
如何系统性地使用索引优化 MySQL 慢SQL，并验证是否有效？

L2：方案
├─ 建立索引结构认知：有哪些优势？
├─ 索引串联整个访问路径：让 where、order by、group by 尽量走同一条 B+树路径
├─ 识别索引失效与低效：是否用了索引？是否效果差？
├─ 通过执行计划定位问题：explain观察
├─ 验证优化收益和副作用：

L3：机制
├─ 索引结构：B+树机制
├─ 索引串联路径：联合索引
├─ 索引失效与低效：单索引生效规则、联合索引最左前缀规则、回表次数、联合索引设计
├─ 执行计划：explain中 type、key、row、filtered、extra 暴露优化器决策
├─ 验证机制：同时验证执行计划、扫描行数、真实耗时、稳定性

L4：机制细节
├─ B+树机制：
	├─ IO 更少：非叶子节点只存 key 和页指针，单页能放更多索引项，扇出更高，树高更低，而树高即IO次数。
	├─ 支持范围查询：叶子节点存放数据或主键值，并通过链表连接，所以范围查询可以顺序扫描。
	├─ 查询稳定：所有查询最终到叶子节点，路径长度相对稳定。
├─ 联合索引机制：
	├─ 联合索引 idx(a,b,c) 的排序方式是先按 a 排，再按 b 排，再按 c 排。从左往右不跳步
	├─ 范围查询之后的字段索引会失效，能可能作为索引下推进行过滤。
	├─ 设计时高频、区分度高、等值字段尽量靠前
├─ 索引有效性原则：
	├─ 索引列不得使用函数、参与运算、隐式类型转换
	├─ 模糊匹配不能使用左模糊
	├─ or 连接字段会失效
	├─ 不得破坏最左前缀原则
├─ 索引低效原则：
	├─ select * 大量回表
	├─ 选择性差，返回行数太多
├─ 执行计划：
	├─ type：访问方式：system > const > eq_ref > ref > range > index > ALL。
	├─ key：实际使用的索引
	├─ rows：预计扫描行数
	├─ filtered 看条件过滤比例
	├─ Extra 中 Using index 表示覆盖索引；Using index condition 表示索引下推；Using filesort 表示额外排序；Using temporary 表示临时表

L5：应用场景

├─ 大分页：避免 LIMIT offset, size 深度跳过，改游标分页或延迟关联。
├─ ORDER BY 慢：让 where 条件和 order by 字段进入合适的联合索引。
├─ COUNT 慢：大表精确 count 不一定靠索引解决，常用缓存、汇总表、异步统计。
├─ JOIN 慢：保证 join 字段类型一致、有索引、小表驱动大表，并控制中间结果。
├─ 范围查询 + 排序：一个索引很难同时完美服务范围过滤和另一个字段排序，需要按业务优先级取舍。
```