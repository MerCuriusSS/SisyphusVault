---
tags:
  - Areas/Coder/基础原理
  - Areas/Coder/javaWeb
category: 技术
status: 加工
project: "[[02 - Projects/后台管理系统「Ruoyi-Vue-Plus」|后台管理系统「Ruoyi-Vue-Plus」]]"
application: 生产问题排查
source: "[[生产环境问题排查完全指南]]"
---
>**笔记不是为了复述书本，而是为了**“存档当下的自己”。如果你的笔记里没有你的思考痕迹、痛苦经历和选择理由**，它就只是一份毫无生命力的说明书，自然无法在未来唤醒你的认知***


## 💥 核心结论
>核心定义是什么? 核心价值在哪里?

### 🔴 核心定义：通过一系列可观测的手段（日志、指标、追踪），定位并解决功能异常、性能瓶颈、系统故障等生产系统问题。

### 🔴 核心价值：及时发现系统问题，保障系统高可用，降低经济和用户损失。

## 🔪 我的见解
>为什么要记录它？它解决了什么我以前解决不了的问题？

### 🔴 记录原因：
1.  只做功能开发，缺乏信息记录，导致用户使用出现问题时无法追查和解决。
2. 不知道信息记录要记什么、怎么记，无法准确还原「案发现场」。
3. 反馈的功能正常但响应缓慢的「性能」问题，无从下手，全凭感觉猜。
4. 无法跟踪用户操作时的一系列请求
### 🔴 解决痛点：
1. 通过建立「日志体系」，准确还原「案发现场」。
2. 通过建立「指标体系」，利用耗时监控、慢查询日志、JVM 工具精准找到性能瓶颈
3. 建立「追踪体系」，通过 TraceId 或 SkyWalking 还原完整调用过程


## ⚡️ 我的重构
>它的底层逻辑是什么？（尝试用最简单的类比解释给外行听）它的结构是什么?

### 🔴 底层逻辑：依靠`日志`、`指标`、`追踪`三大可观测数据，使用「缩小范围法」不断收敛故障边界，最终定位到代码、配置、环境等具体问题。
### 🔴 结构：
#### 1. 日志（Logs）
- **定义**：记录系统“做了什么”的文字内容。
- **意义**：理解用户业务行为、复原「案发现场」。
- **典型内容（记什么）**：
	- ⚠️请求数据（请求类型、路径、入参）【记录案发现场上下文】
	- 用户操作日志（谁、什么时间、做了什么、结果如何）【用户行为审计、结果可视化、错误的初步筛选】
	- ⚠️系统错误日志（异常堆栈、错误信息）【定位具体代码行】
	- ⚠️业务日志（订单创建等业务流程信息）【理解用户行为】
- **使用方法论（怎么记）：**
	- 「**Logback**」作为记录工具
	- 划分日志类型（**「info**」类型记录请求数据、业务日志；「**error**」类型记录业务错误、系统错误信息）
	- **当前+日期滚动**保存策略，**Spring Boot Admin**在线查看和搜索当前日志
	- **异步写入（AsyncAppender）**
	- 「**LogAspect + @Log**」切面记录用户操作日志（操作人、ip地址、请求URL、请求方式、请求参数业务类型【新增/删除/修改】、业务标题、异常信息）
	- **Spring事件** 异步保存用户操作日志
	- **全局异常捕获**并记录信息（异常统一记录请求URL保存到error.log、返回统一错误响应格式）
#### 2. 指标（Metrics）
- **定义**：记录系统运行时“状态如何”的数据内容。
- **意义**：实时监控和告警，定位性能瓶颈、服务可用与否。
- **典型内容（记什么）**：
	- 系统指标（CPU、内存、磁盘）
	- 应用指标（连接池、GC、线程）
	- 业务指标（QPS、请求耗时）
- **使用方法论（怎么记）**
	- 请求耗时：用「**拦截器**」工具在请求前后记录时间统计总耗时
	- 数据库监控：
		- **慢SQL监控**：开启数据库慢查询日志（/var/log/mysql/slow.log），从日志中统计执行时间长的SQL。
		- **连接池（HikariCP）连接情况**：**Actuator**采集指标数据、 Prometheus+Grafana实现可视化和趋势展示
			- 查看活跃连接数（hikaricp.connections.active）
			- 查看等待连接的线程数（hikaricp.connections.pending）
	- JVM监控：（**Actuator** 通过 JMX自动采集 + 接口访问）
		- **JVM内存**（/actuator/metrics/jvm.memory.used）
		- **GC垃圾回收**
			- YoungGC/FullGC 次数（/actuator/metrics/jvm.gc.collection.count）
			- GC 单次耗时 / 总耗时（/actuator/metrics/jvm.gc.pause）
		- **线程**
			- 活跃线程数（/actuator/metrics/jvm.threads.live）
			- 阻塞线程数（/actuator/metrics/jvm.threads.blocked）
	- 系统指标：（**Actuator** 通过 操作系统接口自动采集 + 接口访问）
		- **CPU 使用率**（/actuator/metrics/system.cpu.usage）
		- **系统内存使用**（/actuator/metrics/system.memory.used）
		- **磁盘使用率**（/actuator/metrics/disk.usage）
		- **网络入 / 出流量**（/actuator/metrics/net.bytes）
#### 3. 追踪（Traces）
- **定义**：记录一个请求在分布式系统中的完整调用链路
- **意义**：理解不同服务依赖关系、性能瓶颈分析
- 典型内容（记什么）：
	- 一次请求的完整调用树
- 使用方法论（怎么记）：
	- 集成**skyworking**工具，运行jar包时添加**Agent**
	- skyworking日志配置替代原先配置，添加traceID

## 🚀 实践应用：

### 🟣 问题排查核心思路：
- 明确问题类型
	- **功能错**：结果不对、报错、异常、数据错
	- **性能慢**：超时、卡顿、等待、阻塞
	- **不可用**：服务挂、连不上、起不来
- 划分范围——前端 → 网关 → 应用 → 数据库 → 中间件
- 找证据：
	- **日志**：定位「代码逻辑错误」——操作日志 → 错误日志 → 异常栈 → 定位代码行
	- **指标**：定位「资源性能瓶颈」
		- 慢——耗时日志 → 慢SQL → 连接池 → 线程栈 → 锁等待
		- 挂/不响应——进程是否存活 → 健康检查 → 系统资源（磁盘/内存） → OOM → 死锁
	- **追踪**：定位「全链路瓶颈」——服务依赖、耗时
- 缩小范围：整站 → 服务 → 类 → 方法 → 行



## ⛪ 场景设想
- **场景 A**：功能错——用户反馈"删除失败"
	- 通过操作日志搜集用户删除操作信息——时间、删除结果
	- 通过系统「error」日志查看堆栈——根据时间过滤出日志信息
	- 通过堆栈定位代码位置——获取报错执行的代码行数
	- 结合代码分析原因

- **场景 B**：性能慢——反馈“接口响应慢”
	- 通过系统「info」日志查看响应耗时——根据接口过滤出日志信息
	- 分析慢的原因：
		- SQL查询慢——搜索慢查询日志；
		- 数据量大——是否设置分页；
		- N+1查询——是否循环执行SQL
		- 外部调用慢——是否调用了慢的外部接口
		- 锁等待——是否存在锁竞争

- **场景 C**：系统挂——反馈“服务用不了”
	- 检查服务是否运行——查看进程
	- 查看启动日志

- **场景 D**：内存泄漏——反馈“系统崩溃”
	- 查看java进程占用物理内存情况——物理内存占比大（进程内存=堆内存+堆外内存）
	- 查看交换分区情况——Linux 的内存 =「物理内存 + 交换分区（swap）」；物理内存不足会使用swap
	- 查看JVM堆内存情况——验证泄露三要素：老年代内存使用率不断上升；FullGC频率短时间内增加；FullGC无效；
	- 生成堆快照——生成案发现场的堆内存信息；
	- MAT 分析快照——找到占用内存最多对象
	- 查看Dominator Tree——定位对象引用链
	- 检查业务代码——清理静态变量、关闭资源
	- 修复+验证——堆内存使用率稳定、FullGC恢复正常