## 9:30-10:30（算法）
一、合并两个有序链表
- 初想法：
	- 以链表A作为基准，把链表B的节点插入到A中；
	- 插入一个节点需要同时关注**前驱节点**和**后继节点**，指针操作复杂。
	- 推进不下去，失败告终。
- 问题点：
	- 缺乏“结果反推输入”的思维，只盯着“输入链表要怎么改”，而不是想着“我最终要得到什么”
	- 缺乏对每个推导前提的质疑——是否一定需要修改原链表？思维路径：
		- `if`以链表A为基准，那么链表B往A中插入元素
			- `if headB>headA
				- `if headA.next > headB`,可插入;
				- `if headA.next < headB`,不可插;
			- ...
- 解题思路：
	- 以新链表为基准，每次比较A，B两个链表的值，将较小的插入新链表，推进小的链表进度。
	- 某个链表推进完全后，新链连接剩下的链表。
	- 涉及到`增`操作，使用`虚拟节点`减少边界条件。
	- 核心代码：
	- ```java
	  public ListNode mergeList(ListNode l1,ListNode l2){
	  	  ListNode dummy=new ListNode(null);
		  ListNode head=dummy;
		  ListNode p1=l1;
		  ListNode p2=l2;
		  while(pA!=null&&pB!=null){
			  if(pA.val<pB.val){
				  dummy.next=pA;
				  pA=pA.next;
			  }else{
				  dummy.next=pB;
				  pB=pB.next;
			  }
			  dummy=dummy.next;
		  }
		  if(pA!=null)
			  dummy.next=pA;
		  if(pB!=null)
			  dummy.next=pB;
		  return head.next;
	  }

	  ```
二、反转链表
- 初想法：
	- 