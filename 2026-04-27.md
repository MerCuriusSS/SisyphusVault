## 9:10-12:10

### SQL优化策略（完善）

### B+树查询数据全过程
- 页结构（核心）:[B+树页结构](excalidraw/B+树页结构.md)
- 阐述：
	- **B + 树查找数据分为三个阶段**：根节点遍历非叶子节点、非叶子节点逐层下钻至叶子节点、叶子节点最终取数。
	- **「根节点到非叶子节点」阶段**：先通过页内**槽数组二分查找**，快速定位目标所属数据分组；再顺序遍历该分组内索引项，匹配索引边界区间，拿到对应**下层子页指针**，加载下级节点页。
	- **「非叶子节点到叶子节点」阶段**：重复执行「槽数组二分 → 遍历分组索引项 → 匹配区间 → 获取子页指针 → 加载子页」的逻辑，层层向下路由。
	- **「叶子节点取数」阶段**：依旧依靠槽数组二分定位数据分组，顺序遍历组内行记录，精准匹配目标索引值，读取完整行数据并返回结果。


## 14:00-15:00

### count(`*`) 和count(1) 与 count(字段)的区别
- count(`*`) 等价于 count(1),会统计所有行，count(字段)会筛掉`NULL`值的行数。
- count(`*`)优先选用小值索引（查询损耗小）

### varchar与char的区别
- char定长存储，字符大小不满足预设长度时由空格补充。
- varchar变长存储，按实际字符大小计算，另外多1-2个字节记录实际字符长度


### Mysql如何实现事务
- 总结：通过「RedoLog」、「UndoLog」、「锁」、「MVCC」实现事务的。
- RedoLog保证事务持久性
	- 是什么？预写日志，先写日志后刷数据
	- 为什么？保证「已提交事务」不丢失；顺序IO
	- 怎么做？
		- 将数据的修改/新增操作通过追加的方式写入日志文件中，后续再找机会
- UndoLog保证事务的原子性
- 锁+MVCC保证了事务的隔离性和并发度。