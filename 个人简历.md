### 数据库元数据管理与版本追踪系统

#### 1.项目及工作描述：

> **技术栈**：SpringBoot, JPA, SpringSecurity, 多数据库方言

**项目背景：** 为了解决企业内部多业务线数据资产黑盒化、变更不可控的问题，主导开发了一套统一的数据库元数据管理与监控平台，为下游的数据血缘分析与合规审计提供底层数据支撑。

**主要工作**：
1. **异构海量数据源采集架构**：为MySQL/Oracle/PGSQL等类型数据库抽象统一的元数据适配器。支持采集**500+** 核心异构实例，覆盖近 **18.7万** 张业务表，为下游服务提供元数据支撑。
2. **多级 Hash 极速比对**：针对海量表比对导致的 CPU 飙升与耗时瓶颈，废弃传统双层 List 嵌套遍历。设计“表-字段-索引-约束”多级 MD5 摘要联动哈希算法，实现 **99%** 无变更对象的直接剪枝，配合 HashMap 将差异  时间复杂度降维至 O(1)，比对性能呈指数级跃升。
3. **防 OOM 稳定性治理**：针对极端大表元数据加载导致的内存溢出风险，重构为“游标分页+按批次落地”的流式采集链路，彻底根除大对象长期驻留 JVM 的 OOM 隐患
4. **低侵入与自适应限流防护**：将采集源路由至只读从库并实施凌晨错峰调度实现物理隔离；引入Guava RateLimiter 组件，基于 DB 连接池活跃度和EMA算法检测的 RT 波动实现**自适应背压降级**。
#### 2.面试介绍：


---

### 多源数据同步与NAS落盘系统

#### 1.项目及工作描述：

> **技术栈**：Java 8 + JUC+Kafka Clients

**【简历惊艳版描述】**

**项目背景：** 针对大批量、多源头（API/DB/Kafka）异构数据高频写入 NAS 盘的性能瓶颈与高可用需求，自研了一套轻量级、高并发的离线数据同步落盘引擎。

**主要工作**：
1. **控制面与数据面分离架构**：主导多源异构数据同步引擎开发。采用“控制面（高可用任务调度） + 数据面（异步执行引擎）”分离架构。管控台采用 1主1从 节点部署，负责多数据源连接池维护与任务生命周期管理；底层基于异步线程池动态挂载异构数据采集策略（API/DB/Kafka），实现管控链路与重度 I/O 链路的物理隔离。
2. **数据面异步落盘架构与高吞吐调优**：针对海量 Kafka 数据的落盘场景，重构为“多 Consumer 拉取 + 10万级有界阻塞队列（实现天然背压限流，防 OOM） + 单 Writer 批量刷盘”的异步架构。单机平稳支撑 **8000 TPS** 峰值及日均 **1.5TB** 数据落盘。
3. **磁盘 I/O 深度优化与资源降本**：抛弃单条消息实时的写盘策略，实现无锁化单线程按日/按大小（1GB）滚动切割文件；引入基于时间窗口（1s）的批量 Flush 机制，将底层系统调用次数从 **15 亿次/天骤降至 8.6 万次/天**。
4. **精准 Offset 管理与防丢防重机制**：为解决系统宕机导致的重复消费或数据丢失，设计跨线程的 `ackQueue` 回传机制，严格保障“先落盘、后提交”的 At-Least-Once 语义。落地轻量级断电恢复方案：在应用重启时读取旧文件尾部 16KB 提取各分区 Max Offset 并执行 Consumer Seek，完美消除宕机重启带来的冗余数据。
5. **控制面分布式任务管控演进**：针对多节点部署下的异步任务管控，主导调度机制演进。废弃传统的 DB 定时扫表轮询，重构为基于 **Redis Pub/Sub 的延迟发布订阅模型**。管控台下发指令秒级触达工作节点内存，实现任务启停的 **0 延迟**响应与精准拦截，彻底消除 DB 并发轮询压力。


#### 2.面试介绍：

多源数据同步与NAS落盘引擎是我主导开发的一个项目。其项目背景是，我们需要从不同数据源（特别是kafka）提取海量异构数据，解析并落盘到NAS中供下游分析。我接手并重构后，系统单机稳态支撑6500TPS，峰值能打到8000TPS，每天能稳定完成1.5TB数据的零故障落盘。