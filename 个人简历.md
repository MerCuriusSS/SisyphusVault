### 👑 王牌项目一：企业级数据库元数据管理与版本追踪系统

> **核心标签**：数据治理、底层结构抽象、版本对比状态机
> 
> **技术栈**：SpringBoot, JPA, SpringSecurity, 多数据库方言

**【简历惊艳版描述】**

**项目背景：** 为了解决企业内部多业务线数据资产黑盒化、变更不可控的问题，主导开发了一套统一的数据库元数据管理与监控平台，为下游的数据血缘分析与合规审计提供底层数据支撑。

**主要工作**：
1. **异构海量数据源采集架构**：为MySQL/Oracle/PGSQL等类型数据库抽象统一的元数据适配器。支持采集**500+** 核心异构实例，覆盖近 **18.7万** 张业务表，为下游服务提供元数据支撑。
2. 


---

### 👑 王牌项目二：高并发多源数据同步与NAS落盘引擎 (原“落数工具”)

> **核心标签**：高并发吞吐、JUC锁调优、分布式容错、系统自愈
> 
> **技术栈**：Java 8, JUC (ReadWriteLock), Kafka, 异步调度

**【简历惊艳版描述】**

**项目背景：** 针对大批量、多源头（API/DB/Kafka）异构数据高频写入 NAS 盘的性能瓶颈与高可用需求，自研了一套轻量级、高并发的离线数据同步落盘引擎。

**主要工作**：
1. **异步落盘架构与高吞吐调优**：基于 Java 21 设计高吞吐异构数据同步引擎，针对原多线程直接写文件导致的锁竞争与乱码瓶颈，重构为“多 Consumer 拉取 + 10万级有界阻塞队列（实现天然背压限流，防 OOM） + 单 Writer 批量刷盘”的异步架构。单机平稳支撑 **8000 TPS** 峰值，日均完成 **1.5TB** 数据零故障 NAS 落盘。
2. **磁盘 I/O 深度优化与资源降本**：抛弃单条消息实时的写盘策略，实现无锁化单线程按日/按大小（1GB）滚动切割文件；引入基于时间窗口（1s）的批量 Flush 机制，将底层系统调用（Syscall）次数从 **15 亿次/天骤降至 8.6 万次/天**，使得单机 CPU 负载从均值 2-3 核断崖式降低至 **0.5 核以内**。
3. **精准 Offset 管理与防丢防重机制**：为解决系统宕机导致的重复消费或数据丢失，设计跨线程的 `ackQueue` 回传机制，严格保障“先落盘、后提交”的 At-Least-Once 语义。落地轻量级断电恢复方案：利用应用启动钩子（Shutdown Hook / Recovery）扫描旧文件尾部 16KB 提取各分区 Max Offset 并执行 Consumer Seek，完美消除宕机重启带来的冗余数据。
4. **分布式任务管控演进**：主导集群（多节点）任务中断机制从“DB 定时扫表”向“Redis Pub/Sub 延迟发布订阅”模型演进，彻底消除定时任务带来的调度延迟与 DB 轮询压力，实现多节点任务调度的 **0 延迟**与精准拦截。

---

### 💡 面试官视角的补充建议

拆分成两个项目后，你在面试时的话术需要做一点微调。

如果面试官问：**“这两个项目是同一个时期的，它们之间有联系吗？”**

你可以这样高情商回答：

> “它们是我在同一家公司负责的数据基建体系的两个重要拼图。元数据系统解决的是**‘数据结构是什么、怎么变’**的合规与治理问题；而NAS落数引擎解决的是**‘海量数据怎么高效、安全地流转落地’**的性能问题。这两者的结合，帮公司打通了从数据定义到数据同步的完整闭环。”

这样一说，你的架构视野直接拉满。

关于2023年至今的这两大项目，现在这个版本的包装你是否满意？如果没问题，我们要继续向你的**【商城项目】**（需要大规模删改重塑）或者**【电网项目】**（15s降到3s的 CompletableFuture 实战）开刀了。你想先处理哪一个？