### 数据库元数据管理与版本追踪系统

#### 1.项目及工作描述：

> **技术栈**：SpringBoot, JPA, SpringSecurity, 多数据库方言

**项目背景：** 为了解决企业内部多业务线数据资产黑盒化、变更不可控的问题，主导开发了一套统一的数据库元数据管理与监控平台，为下游的数据血缘分析与合规审计提供底层数据支撑。

**主要工作**：
1. **异构海量数据源采集架构**：为MySQL/Oracle/PGSQL等类型数据库抽象统一的元数据适配器。支持采集**500+** 核心异构实例，覆盖近 **18.7万** 张业务表，为下游服务提供元数据支撑。
2. **多级 Hash 极速比对**：针对海量表比对导致的 CPU 飙升与耗时瓶颈，废弃传统双层 List 嵌套遍历。设计“表-字段-索引-约束”多级 MD5 摘要联动哈希算法，实现 **99%** 无变更对象的直接剪枝，配合 HashMap 将差异  时间复杂度降维至 O(1)，比对性能呈指数级跃升。
3. **防 OOM 稳定性治理**：针对极端大表元数据加载导致的内存溢出风险，重构为“游标分页+按批次落地”的流式采集链路，彻底根除大对象长期驻留 JVM 的 OOM 隐患
4. **低侵入与自适应限流防护**：将采集源路由至只读从库并实施凌晨错峰调度实现物理隔离；引入Guava RateLimiter 组件，基于 DB 连接池活跃度和EMA算法检测的 RT 波动实现**自适应背压降级**。
#### 2.面试介绍：

**数据库元数据管理与版本追踪系统**是我主导开发的另一个项目，主要应对公司数据资产越来越混乱、库表字段变更没有版本管控、不可追溯的问题。核心功能是统一管理全公司数据库元数据以及版本变更轨迹，为后续整体的全链路数据血缘、安全审计做好支撑。目前该系统已经稳定管理500多个异构数据库实例（涵盖mysql、oracle、sqlserver、pgsql）等，覆盖了近18.7万张核心业务表。

这个项目中，在整体设计上，需要思考以下几个点：
1.异构数据库系统表结构不同，如何抽象公共规则进行适配？
2.元数据采集这种偏后勤任务，如何不影响线上业务前提下实现高频词精准采集？
3.字段级的版本变更


---

### 多源数据同步与NAS落盘系统

#### 1.项目及工作描述：

> **技术栈**：Java 8 + JUC+Kafka Clients

**【简历惊艳版描述】**

**项目背景：** 针对大批量、多源头（API/DB/Kafka）异构数据高频写入 NAS 盘的性能瓶颈与高可用需求，自研了一套轻量级、高并发的离线数据同步落盘引擎。

**主要工作**：
1. **控制面与数据面分离架构**：主导多源异构数据同步引擎开发。采用“控制面（高可用任务调度） + 数据面（异步执行引擎）”分离架构。管控台采用 1主1从 节点部署，负责多数据源连接池维护与任务生命周期管理；底层基于异步线程池动态挂载异构数据采集策略（API/DB/Kafka），实现管控链路与重度 I/O 链路的物理隔离。
2. **重构异步执行引擎与高吞吐调优**：针对海量 Kafka 数据的落盘场景，重构为“多 Consumer 拉取 + 10万级有界阻塞队列（实现天然背压限流，防 OOM） + 单 Writer 批量刷盘”的异步架构。单核平稳支撑 **8000 TPS** 峰值及日均 **1.5TB** 数据落盘。
3. **磁盘 I/O 深度优化与资源降本**：抛弃单条消息实时的写盘策略，实现无锁化单线程按日/按大小（1GB）滚动切割文件；引入基于时间窗口（1s）的批量 Flush 机制，将底层系统调用次数从 **15 亿次/天骤降至 8.6 万次/天**。
4. **精准 Offset 管理与防丢防重机制**：为解决系统宕机导致的重复消费或数据丢失，设计跨线程的 `ackQueue` 回传机制，严格保障“先落盘、后提交”的 At-Least-Once 语义。落地轻量级断电恢复方案：在应用重启时读取旧文件尾部 16KB 提取各分区 Max Offset 并执行 Consumer Seek，完美消除宕机重启带来的冗余数据。
5. **控制面分布式任务管控演进**：针对多节点部署下的异步任务管控，主导调度机制演进。废弃传统的 DB 定时扫表轮询，重构为基于 **Redis Pub/Sub 的延迟发布订阅模型**，实现任务启停的 **0 延迟**响应与精准拦截，彻底消除 DB 并发轮询压力。


#### 2.面试介绍：
>误区警惕：
>1）单节点≠单核单线程——单核6500常规TPS，那么单节点使用3核心的情况下，节点的TPS应该是6500`*`3的体量
>2）多线程更能释放网卡带宽性能——网卡带宽是硬件极限，应用受协议停等、RTT空挡、内核缓冲队列等，接收的吞吐量有损耗；单线程更CPU串行处理、内核调度开销限制、永远跑不满物理吞吐

**多源数据同步与NAS落盘引擎**是我主导开发的一个项目。这个项目**主要解决**企业内部不同数据源到NAS盘的海量同步问题。在**架构设计**上采用了经典的“控制面与数据面分离”的模式：外部是一主一从高可用部署的控制台，专门负责数据源连接和任务生命周期调度；内部是一个异步执行引擎，负责根据任务指令，拉起对应的采集策略实现数据按日落盘。整个系统每天能扛住1.5TB的数据。

在这个架构下，控制面需要频繁向数据面**下发“启动/中断”等指令**，因为是**多节点部署**，所以早期采用“**数据库定时扫表**”的策略做节点间的任务状态同步。但这种方式不仅**有延迟**，还**浪费数据库性能**。所以后面我对控制面的**管控链路做了优化**，引入了“**Redis Pub/Sub 延迟发布订阅模型**”，只要在**控制台点击取消**，指令通过**redis广播**，工作节点收到后直接**匹配本地内存中的异步线程进行中断**，做到了跨节点调度的**0延迟和数据库0压力**

当然，控制台调度得再好，最终还是需要底层数据面完成实际落数逻辑，特别是**针对kafka这种高吞吐的数据源**，如何在单节点榨干 I/O 性能，才是这个项目最大的难点。在 Kafka 专属的落盘策略中，原先采用的是最简单的“单消费者拉取数据->文件写入”的模型，但无法应对后面数据量激增引起的lag不断增加，于是开始引入多线程消费，但常规多线程直接写文件容易导致锁竞争与文件乱码问题。基于以上情况，最终设计了一套**‘多 Consumer 并行拉取 + 10万级有界队列 + 单 Writer 批量刷盘’**的异步模型。

性能上：使用**阻塞队列解耦**了poll（等网络）和write（等磁盘）的同步方式，实现异步处理，无需彼此等待，充分释放执行效率；使用**按时间间隔批量刷盘**取代单条刷盘，让系统调用次数从15亿次骤降到8万多次，TPS天花板提升10-30倍。

可用性上：使用**单线程写入文件**，消除多线程并发写乱码的问题；使用**有界队列实现天然背压**，防止内存无限堆积，造成OOM；使用**按固定大小方式切割极大文件**，防止下游直接读取文件出现OOM；

可靠性上：使用**先写文件后提交offset**的手动提交方式，保证at-least-once语义，数据不丢失；使用任务启动时**文件尾部反查offset**，防止提交前宕机重启后重复消费数据；