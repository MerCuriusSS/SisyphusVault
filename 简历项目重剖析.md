#人生/求职 
## 一、数据库元数据采集和监控工具
### 业务流程：
1. 获取对应数据库类型的数据库链接
2. 执行查询元数据SQL获取结果集（表、字段、约束、索引等）
3. 对比查询结果集和本地结果集
4. 差异更新并形成新版本
5. 最新数据、数据差异入库操作
6. 结束
### 难点：
1. 不同数据源元数据采集的方言
	1. **MySQL**：【TABLE_SCHEMA：数据库名；TABLE_NAME：表名】
		1. 库查询：SHOW DATABASES;
		2. 表级：INFORMATION_SCHEMA.TABLES【TABLE_SCHEMA，TABLE_NAME】
		3. 字段级：INFORMATION_SCHEMA.COLUMNS
		4. 约束级：INFORMATION_SCHEMA.KEY_COLUMN_USAGE
		5. 索引级：INFORMATION_SCHEMA.STATISTICS
	2. **SqlServer**：（库名.dbo.tableName）
		1. 库查询：sys.databases
		2. 表级：sys.tables 、sys.extended_properties 【{schema}.{tableName}】
		3. 字段级：sys.columns
		4. 约束级：INFORMATION_SCHEMA.TABLE_CONSTRAINTS、INFORMATION_SCHEMA.CONSTRAINT_COLUMN
		5. 索引级：sys.indexes
	3. **Oracle**：（用户名等价于database）
		1. 库查询：dba_users
		2. 表级：all_tables【owner、table_name】
		3. 字段级：dba_tab_columns
		4. 约束级：dba_constraints
		5. 索引级：dba_indexes
	4. **PostgreSQL**：（库.public.表名）
		1. 库查询：pg_database
		2. 表级：pg_tables
		3. 字段级：information_schema.columns
		4. 约束级：information_schema.table_constraints
		5. 索引级：pg_indexes
2. 版本比对表设计：


🔴当前数据（表字段为例）：**meta_table_field**

结构：
- 主键
- 关联表id
- 具体采集字段
- 审计字段

|字段|类型|说明|
|---|---|---|
|id|BIGINT PK|主键|
|meta_table_id|BIGINT|关联 meta_table.id|
|field_name|VARCHAR(128)|字段名，唯一，同一个表内不能重复|
|field_type|VARCHAR(256)|完整字段类型 varchar (64)|
|field_comment|VARCHAR(512)|字段注释|
|has_default_value|TINYINT NOT NULL DEFAULT 0|0 无默认值，1 存在默认值，区分 null 语义|
|default_value|VARCHAR(1024)|默认值|
|is_nullable|TINYINT NOT NULL DEFAULT 0|是否允许 null|
|field_sort|INT NOT NULL DEFAULT 0|字段排序位置|
|update_time|DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP||
🔴版本变化数据记录（表字段为例）：**meta_table_field_change_his**

结构：
- 主键
- 关联表id
- 版本信息（当前版本号、变更类型、具体字段变更新旧值）
- 审计字段
变更参照key（字段名称）变化 ：**等价于旧字段删除、新字段新增**

| 字段                                        | 说明                   |
| ----------------------------------------- | -------------------- |
| id                                        | 主键                   |
| meta_table_id                             | 关联元数据表 ID            |
| version_no                                | 本次变更所属版本号            |
| field_name                                | 本次操作的字段名             |
| **old_field_type / new_field_type**       | **类型前后**             |
| **old_field_comment / new_field_comment** | **注释前后**             |
| **old_default_value / new_default_value** | **默认值前后**            |
| change_operate                            | 1 新增字段 2 修改字段 3 删除字段 |
| operator_id                               | 操作人                  |
| create_time                               | 变更时间                 |
🔴前端展示数据类型：

|字段名|变更项|旧值|新值|
|---|---|---|---|
|name|字段类型|varchar(50)|varchar(100)


3. 代码实现方式：
	1. **策略模式**定义统一的元数据查询接口（统一行为，不同实现）
	2. **简单工厂模式**根据数据库类型自动选择对应的策略（创建对象，选择策略）

### 架构优化点：
1. **低侵入调度**：错峰采集；
	1. 将数据库主从分离与从库采集，保证即使出现慢查询和CPU飙升的情况，也能与主库实现完全的物理隔离
	2. 自适应限流和降级，通过组件的限流器（Guava RateLimiter/Sentinel）动态监测当前数据库连接池活跃度或者响应时间，当响应时间变长或报错率增加，自动触发降级（休眠等待或降低拉取频次）[数据库元数据查询限流器实现源码](04%20-%20Resources/数据库元数据查询限流器实现源码.md)
2. **防OOM设计**：针对极端大表元数据加载导致的内存溢出风险，重构为“游标分页+按批次落地”的流式采集链路。（分页查表->逐表查字段/索引/约束->写入快照->释放表数据->继续下一张表） [数据库元数据查询OOM场景以及优化](数据库元数据查询OOM场景以及优化.md)
3. **优化比对逻辑**：使用map替代本地与远端双层list嵌套比对逻辑，避免CPU无效遍历以及大对象长期驻留内存【非多表库进行全量增删比对(远端list->map,map对比本地list)；多表库则直接将远端表落地到表中，用数据库做集合差异】
4. **Hash摘要对比设计**：采用表级 + 字段集 + 索引集 + 约束集多级 MD5 哈希联动机制，先通过哈希值做极速变更筛查，仅对哈希不一致的对象执行精细化字段级差异比对，99% 无变更表可直接跳过。（各级别预先排序，消除只更换字段顺序的无效修改情况）[元数据采集简化流程和优化流程图](excalidraw/元数据采集简化流程和优化流程图.md)


> [!important] 【初级程序员思维】=>【**架构师防御性思维模型**】
>1. 量级推演（what if 放大100倍）：内存是否会爆？CPU是否会拉满？
>2. 爆炸半径隔离（不相信任何外部依赖，不觉得自己绝对安全）：会不会把别人拖死？
>3. 无用功过滤（架构设计的最高境界不是“算得快”，而是“尽量不算”）：没用的东西我能不能不算



## 二、多源数据同步与NAS落盘引擎工具
### 业务流程：
1. 新增数据源（ip、port、接口地址/用户名&密码）
2. 新增任务（绑定数据源、解析器）
3. 新增解析器
	1. API解析器：访问接口获取数据->解析数据->写入文件
	2. 数据库解析器：访问数据库查询表数据->解析数据->写入文件
	3. kafka解析器：消费kafka记录->解析数据->写入文件
4. 【执行】任务
	1. 生成任务详情
	2. 【执行/重跑】：异步调起线程【@Async】（使用解析器执行解析工作）
5. 返回任务结果，内容如下：
	1. 任务id
	2. 调用时间
	3. 异步线程结果凭证/票据 `future`
6. 本地map记录任务结果
7. 【取消】任务
	1. 获取任务详情
	2. 本节点：map获取线程凭证，取消任务
	3. 非本节点：
		1. 无凭证，查取消记录表、新增取消记录
		2. 由其他节点定时任务扫取消记录表，执行取消任务操作

### 架构优化点：
1. ==消息可靠性==
	1. **前**：kafka默认为消息自动提交策略，消息会丢失——`poll数据->自动提交->写入文件`（提交在前，写文件在后，崩溃丢数据）
	2. **改**：[落数工具核心代码](04%20-%20Resources/落数工具核心代码.md)
		1. 手动提交——`poll数据->写入文件->手动提交`（写文件在前，提交在后，崩溃不丢数据）；
		2. 遗留了「消息重复消费」问题(**保证at-least-once语义**)——`写文件后，提交前发生宕机`；
2. ==多消费者并发消费== 
	1. **前**：单消费线程消费——`poll(等1s)->处理500条数据->写文件->换日切换判断->poll(等1s)...`
		1. 缺陷：IOPS直接打满（200次/s）
	2. 废弃方案：多消费线程消费「读写锁」
		1. 流程：`poll(等1s)->处理500条数据->并行写入文件(读锁)->换日切换判断(写锁)->poll(等1s)`
		2. 最大缺陷：多线程并发写入同一个文件「必然」出现乱码问题，文件写入只能由单线程实现。
	3. **后**：多消费者线程+阻塞队列+单线程文件写入【单核2.4万TPS的核心：队列解耦**拉取与写入速率**+批量刷盘降低**IPOS和系统调用**】
		1. 优势：
			1. 多消费者线程并行拉取kafka数据，保证数据接收不停滞（commitSync），队列有数。
			2. 有界阻塞队列保证kafka数据存放的并发安全、解耦网络与磁盘速率差，不互相等候，实现削峰蓄水和背压(**backpressure**)限流目的
			3. 单线程写入保证输出文件不乱码
		2. 流程：[落数工具核心代码](04%20-%20Resources/落数工具核心代码.md)
			1. 外层：依次执行创建消费者线程多个、创建文件写入线程、开启消费者线程和文件写入线程分别执行**消费者任务**和**文件写入任务**。并实现了关闭JVM后对消费者和文件写入线程的优雅停止钩子 
			2. **消费者任务**：手动提交偏移量->间歇拉取数据(1s)->放入阻塞队列->异常中断处理的过程
			3. **写入文件任务**：从队列获取最多500条数据->数据写入文件以及换日判断->完成后偏移量集合回传给消费者。偏移量集合通过各消费者私有的ackQueue实现**消费者线程**和**文件写入线程** 2端的数据互通，按消费者、分区依次分组，同分区合并最大偏移量。
3. ==降低重复规模==：
	1. 落数文件幂等：
		1. 模式：扫描文件当前最大`offset`，让consumer 跳到`offset+1`的位置继续消费。
		2. 流程:[落数工具核心代码](04%20-%20Resources/落数工具核心代码.md)
			1. 扫描`{today}`目录下所有文件
			2. 读取每个文件的最后N行:`RandomAccessFile`对象+`seek`功能+`fileSize-readDataSize`起读点。
			3. 提取唯一标识（partition&offset）
			4. 按分区汇总，取最大offset
			5. consumer根据所属分区` seek`到 (max_offset+1)跳过已写入的消息
	2. 下游幂等：
		1. 模式：`通用模式：记录唯一消息记录->独立存储已处理记录->处理前先查记录`
		2. 参考方式：
			1. 数据库限制：设定消息唯一索引+`INSERT ... ON DUPLICATE KEY / upsert`
			2. 代码逻辑处理：提取文件内容+` HashSet<(topic,partition,offset)> 内存去重`
4. ==单日文件大小==：
	1. 前：单日文件数据量过大（1TB左右），下游服务直接提取容易出现OOM
	2. 后：文件按特定大小进行切割，滚动生成。
		1. 流程：[落数工具核心代码](04%20-%20Resources/落数工具核心代码.md)
			1. 先作换日判断、再作文件大小上限判断
			2. 每次写入数据后更新字节计数
			3. 生成的新文件通过seq后缀标记，累加制；换日后重置seq为0
5. ==缓冲区使用==
	1. 前：每次写入都刷盘，磁盘IO效率无法充分利用[磁盘IO常识](03%20-%20Areas/磁盘IO常识.md)
	2. 后：设置缓冲区+固定刷盘频率（1s），**减少磁盘IOPS负担->减少CPU触发系统调用次数->消除CPU瓶颈。**
		1. 流程：[落数工具核心代码](04%20-%20Resources/落数工具核心代码.md)
			1. bufferWriter内置8kb缓冲区
			2. 固定刷盘频率（1s）：换日/切文件主动刷
6. ==分区rebalance预防==
	1. 前：队满阻塞导致消费者拉取受阻，超过`max.poll.interval.ms`设置时间后，消费者集体停止消费，触发分区rebalance。
	2. 后：`offer()+超时`替换`put()`，根据队满标识舍弃本轮拉取数据，随后重新拉取数据（offset未提交，数据仍会继续推送）
7. TPS极限压测：
	1. 内存挡板压测——本地生产数据，屏蔽kakfa拉取数据的影响，让队列永远满载，让写入线程跑满一个CPU的量，每次刷盘后记录处理的数据条数。跑满1min后对60次结果取平均值，测出本地TPS为25万/s。


## 三、AI零代码应用生成平台

### 主要功能：

- 通过自然语言生成应用，支持可视化编辑、AI 对话式修改、一键部署分享
- 用户、应用、监控等后台管理能力

### AI对话核心流程

- 用户输入初始化提示词
- 后端通过 LangChain 接入LLM、定义系统提示词、接收用户输入提示词
- 请求 LLM
- 接收 LLM 输出结果
- 网页内容解析&文件保存
- 前端显示输出内容

### 优化补充：

- （**提高用户体验**）输出方式替换为流式输出，打字机形式。
- （**默认无状态**）新增记忆模块，保存用户的对话历史（MySQL持久化、Redis缓存）
- （**支持复杂工程化项目**）Tool-Calling 方式实现 Vue 模式的复杂工程化项目生成
- （**Vue项目源码需构建**）操作命令行工具执行`NPM`命令，完成构建操作
- （**按需智能选择生成方式**）LLM充当动态路由
- （**提示词存在风险**）护轨预防提示词注入攻击和越狱
- （**用户需求模糊**）四维度网页模板 RAG 提示词增强

### 优化具体实现：

#### 流式输出：

1. 响应数据类型为 `SSE`：`text/event‑stream`
2. LLM使用流式模型
3. Reactor `Flux`作为 `LLM`与 `响应` 的连接桥梁
4. 每段流完成json封装解决空格丢失问题。

#### 对话历史:

1. 每轮与AI对话保存到 MySQL，记录完整的对话历史
2. Redis 作为LLM聊天记忆存储载体，记忆滑动窗口大小为20条（10轮）对话，首次对话/Redis过期时从MySQL重新载入
3. 窗口溢出时，最旧一条消息调用LLM生成摘要，标记为`SystemMessage`，后续的每次溢出按照`新摘要=旧摘要+旧消息`的方式增量更新。
4. 压缩上下文

#### Tool Calling 构建工程化项目

1. 为LLM 制定  `Vue3 开发` 系统提示词
2. 编写`文件写入`、`文件内容读取`、`文件删除`、`文件内容替换`、`文件目录查看` 工具代码
3. 声明各类工具功能和参数含义，如文件内容替换，功能是“修改文件内容，用新内容替换为指定旧内容”，输入参数分别为：文件相对路径、要替换的旧内容、替换后的新内容。
4. 工具注入到 LangChain中
5. LLM 驱动 LangChain 完成工具调用，解析输出结果：AI说的话、工具调用记录、工具调用结果
6. 对话结束后调用`npm`命令构建项目

#### 工程化项目构建部署

1. 使用 `Hutool` 进程命令调用工具，根据不同操作系统调用`npm`命令完成Vue源码到html文件的构建
2. 执行部署操作时，复制文件到部署目录
3. 完成访问URL到部署目录文件的映射

#### 生成类型动态路由

1. 接收用户初始提示词并调用LLM
2. 语义解析后选择 纯Html或者Vue项目之一的生成模式

#### Guardrail 保证 LLM 输出安全

1. 实现`InputGuardrail`接口，在输入端通过正则方式校验用户提示词合法性
2. 维度涵盖：指令覆盖、系统提示词窃取、身份切换。
3. 每次接收用户输入时，在LLM接手前先做一层过滤，条件满足放行，否则返回输入不合法提示。

#### 四维度网页模板 RAG 提示词增强

**存**：网页生成模板
**分片**：4个正交维度+同一维度下的关键字

1. 以`yaml`格式编写网页模板，按 `网页目的`、`数据交互`、`用户体验`、`项目类型` 4个正交维度分片，按匹配关键字进行向量编码。
2. 启动时将模板库存入向量库。
3. 在线检索时，对用户提示词进行向量转化，与向量库数据进行匹配，抽取相似度>0.8的前2模板返回。
4. 拼接到原始提示词中，再发给LLM进行处理

