## 赞同科技有限公司项目经历

### 一、企业级数据库元数据管理与版本追踪系统

**项目背景：** 为了解决企业内部多业务线数据资产黑盒化、变更不可控的问题，主导开发了一套统一的数据库元数据管理与监控平台，为下游的数据血缘分析与合规审计提供底层数据支撑。

**技术栈**：Springboot、JPA、SpringSecurity、多数据库方言

**工作**：负责各应用下mysql、oracle、sqlserver、pgsql等数据库的元数据信息定期采集开发，包括数据库信息、表信息、字段信息、约束信息、索引信息、分区信息等，记录变更版本信息

**执行流程**：
1. 获取对应数据库类型的数据库链接
2. 执行查询元数据SQL获取结果集
3. 对比查询结果集和本地结果集（表级、字段级
4. 差异更新并形成新版本
5. 入库操作
6. 结束

**本地表设计**：
#### 基础信息表
- 库信息表
	- 数据库名称
	- 数据库类型
	- 数据库版本
- 表信息表
	- 表注释
	- 表英文名
	- DDL
- 字段信息表（带table_name）
	- 字段名称（COLUMN_NAME）
	- 中文注释
	- 数据类型
	- 默认值
- 约束信息表（带table_name）
	- 字段名称（COLUMN_NAME）
	- 约束名称
	- 引用表名
- 索引信息表（带table_name）
	- 字段名称（COLUMN_NAME）
	- 索引名称
	- 索引类型
#### 版本信息表（按模块分类展示）
- 不可变元数据（【库维度】库名称、【表维度】表名称、【字段维度】字段名称、【索引维度】索引名称）
- 变更类型（增/删/改）
- 变更前内容（可变元数据）
	- 【库维度】：库类型、库版本
		- 增：(空)->xx
		- 删：xx->(空)
		- 改：xx->yy
	- 【表维度】：表注释、DDL
	- 【字段维度】：
		- 字段类型、字段注释...
	- 【约束维度】
		- 约束名称、引用表名...
	- 【索引维度】
		- 索引名称、索引类型...
- 变更后内容

#### 数据来源表：
- 库：**INFORMATION_SCHEMA**
- 表
	- 表信息：**TABLES**
	- 字段：**COLUMNS**
	- 约束：**KEY_COLUMN_USAGE**
	- 索引：**STATISTICS**

### 二、高并发多源数据同步与NAS落盘引擎工具

技术栈：JUC、java8
项目描述：从上游不同数据源中提取源数据，通过解析后写入文件并落到nas盘中。

执行流程：
1. 新增解析器实现解析逻辑
2. 新增数据源（选择采集的数据源是API/数据库/kafka）
3. 新增任务：绑定数据源和解析器、初始化写入文件目录
4. 启动解析任务

设计时需要考虑几个重要问题：
其一是任务执行时是一个耗时操作，不应该被关联到主流程中，因此这部分应该设计为异步的；
其二是任务的执行情况需要被主线程感知，所以这部分解决方案需要添加任务状态标识，显示任务状态；
其三是有针对KAFKA类型的任务，本身是轮询的，有时需要视情况停止，因此执行的任务需要有中断操作；

此外还需要解决一些难点问题：
①在多节点部署的情况下如何保证能准确取消任一节点中的任务？解决方案是将取消任务的信息入库，外加各节点定期扫表，同时每个节点在内存中维护一张任务表，根据取消任务信息遍历内存任务表，中断存在于内存表中的任务。
②另外应用挂掉后，启动的任务也会一并停止，如何保证应用重启后，运行中的任务也能自动被拉起并继续执行？解决方案就是实现一个ApplicationRunner类，在springboot容器启动后查询数据库正在执行的任务，并重新拉起属于自己节点运行的任务。
③任务执行时间有限制，如何对超时执行的任务自动取消掉？解决方案是借鉴了看门狗机制，用一个定时任务检查当前时间与任务启动时间的差值是否超过时间限制，超过则将该任务取消。

使用读写锁解决了多线程消费kafka的问题
①对于执行的kafka任务，我们一般是将获取的kafka记录解析后写入到一个文件中，在换日的时候，把文件迁移到另一个目录中，再在新日期目录下创建一个新的文件继续执行写入操作。
②因此在多线程消费时，在换日迁移文件时，就涉及到多个线程如何处理同一个文件这一共享资源的问题，也就是线程同步。解决线程同步的方式就是加锁。当时的想法是添加一个普通对象锁，这就需要考虑2种情况，一种情况是需要保证满足迁移条件的各线程是互斥的，这种情况要求在迁移条件前加锁；另一种情况是保证满足迁移条件的线程与写入文件的线程是互斥的，这种情况要求在写入数据前加锁。但这样的加锁方式会导致的结果是多线程写入文件时是串行执行的，无法提高消费效率。也就是说普通对象锁并不能同时满足实现多线程写入文件时并行，文件被迁移时互斥2种情况。此时问题就转化为如何找到一种锁对象能同时实现并行和互斥2种效果。而读写锁恰恰能满足这种条件。其中多线程写入文件时使用读锁，根据读-读不互斥的特性实现多线程并行；在文件迁移时使用写锁，根据读-写互斥的特性实现线程安全。
由此可以得出最终流程是：
①各线程创建消费者并轮询获取kafka线程
②各自维护自己的文件流并将数据写入文件，在写入文件之前加入读锁，实现并行
③满足换日条件下，迁移目录之前加入写锁，在执行迁移操作，实现线程安全；迁移时还需关闭其他文件流，迁移后再为其他线程创建新的文件流

### 三、数字化运营管理系统
技术栈：SpringBoot+JPA+SpringSecurity
项目描述：银行总行及各级分支机构，实现业务指标数据采集展示与决策支撑、内部业务流程线上审批、后勤业务管理的一体化综合管理平台
本人工作：
1.将上游加工后的数据采集到本地数据库并展示给前端页面
2.流程开发工作
3.行内人员印章、排班等内部业务的常规CRUD开发



【灵魂拷问】回复：
### 企业级数据库元数据管理与版本追踪系统
1. **规模与频次：** 你们接入了多少个数据库实例？总表数量大概多少？采集频次是怎样的？
**数据库实例**：500+个
**总表数量**：187500
**采集频次**：一周一次

2. **性能影响：** 采集元数据（尤其是在业务高峰期）对线上源数据库的性能有影响吗？你是怎么控制采集速率或隔离影响的？
没有影响；采用错峰采集的方式隔离影响，固定周六凌晨3点启动

3. **比对与版本（核心）：** 你提到“对比查询结果集和本地结果集差异更新”。如果一个库有上万张表，你是全量拉到本地比对吗（很容易OOM）？有没有用到类似Hash摘要比对或者Binlog解析的增量方案？
目前的手段确实全量拉到本地比对；没有用到Hash摘要或者Binlog解析的增量方案

### 高并发多源数据同步与NAS落盘引擎工具
1. **量化指标：** 这个引擎每天同步的数据量级是多少（GB/TB级别，还是千万/亿级条数）？单机吞吐量（TPS）能达到多少？
一天同步的数据量约为50GB左右；单机吞吐量没测过，只清楚生产环境的资源是8C32G

2. **Kafka多线程消费与Rebalance：** 你说多线程消费Kafka并写入NAS。由于写NAS属于重I/O操作（耗时较长），你是怎么防止Kafka消费超时导致频繁Rebalance的？你的Offset是自动提交还是手动提交？如何保证断电重启时数据不丢失且不重复写入？
没有防止Kafka消费超时导致频繁Rebalance的认知，因为我不知道kafka消费超时的触发条件，消费过程不是使用while(true)一直消费吗，一直消费何来超时？
offset是自动提交的
由于offset是自动提交的，所有无法保证断电重启时数据不丢失且不重复写入。

3. **分布式任务调度太“土”：** 你说用各节点定期扫表来取消任务。在多节点下，频繁扫表会对DB造成巨大压力。当时为什么没有考虑用ZooKeeper的节点监听，或者Redis的Pub/Sub发布订阅模式来实现任务的广播取消？如果是历史包袱，你现在的优化思路是什么？
是历史包袱原因，使用“土”办法而不是用ZooKeeper是因为zookeeper框架太重量，在这种小场景下，使用定时任务扫表的方式更轻便；如果让我来优化的话，我会采用Redis的Pub/Sub发布订阅模式。具体如下：在启动任务生成任务id时，向redis发送一条延迟消息：xx分钟后发送taskID（xx分钟表示任务超时时间）。等xx分钟后，redis作为发布者将taskID发送到各个节点中，各节点查看本地map是否匹配taskID对应的线程，有则执行中断，无则忽略。这个方案的核心优势在于1.消除了定时任务带来的“延迟”缺点，实现0延迟；2.全节点不再多次扫数据库表，实现0压力。


4. **NAS写异常处理：** 如果节点宕机（ApplicationRunner拉起前），NAS盘里正好有一半写了一半的脏数据文件，重启后你是怎么做文件清理和断点续传的？
重启后不做文件清理，直接实现内容追加。


### 数字化运营管理系统（银行OA）
没有接触过复杂的工作流引擎
也没有针对大数据量报表的SQL优化经验