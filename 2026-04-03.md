## 10:00-11:00
二叉树
- 定义
- 种类
- 组成部分
- 遍历方式
	- 递归遍历
		- 前序
		- 中序
		- 后序
	- 层序遍历
- 模板代码

## 11:00-12:00
concurrentHashMap1.8 扩容机制（Transfer）的基本原理
- 定义：操作多个业务线程（执行`put/remove`等操作）实现数组的协助扩容和数据迁移
- 价值：提高扩容效率
- 组成部分：
	- **sizeCtl**：记录`扩容状态`和`执行扩容线程情况`【>0表示扩容阈值，<0表示正在扩容以及标记的扩容线程个数】
	- **stride**：每个线程迁移数据的负责区间（步长），受CPU个数影响，且>=16【按`段`而不是`桶`分配是为了减少迁移时线程竞争】
	- **transferIndex**：区间分配的结束下标。初始值为旧容量，从后往前分配。
	- **ForwardingNode**：桶完成迁移的标志，标记后后续读写转向新桶。
- 执行过程：
	- 触发扩容时生成N`*`2的新数组
	- 初始化`transferIndex`为N；
	- 计算步长（stride）