## 10:00-11:00
### 递归思维优化与巩固
- 分析结果类型：「自上往下」还是「自下往上」
	- 自上往下：遍历型思维——问题拆解为「遍历」的更小同构子问题
	- 自下往上：分解问题型思维——问题拆解为「返回结果」的更小同构子问题 与 X条件
- 求解原问题：
	- 自上往下：入栈前&出栈后处理、末端条件判断。
	- 自下往上：原问题与子问题与X条件的关系
- 提供基准条件作为递归出口
	- 自上往下：`NPE`之类的简单边界条件。
	- 自下往上：最小子问题的答案。


## 11:00-12:00

### 遍历型递归题型的巩固与思路验证

#### 二叉树的所有路径
- 结果类型判断：从根节点到叶子节点的所有值->自上而下->遍历思维
- 递归思维子问题拆解：二叉树遍历递归模板:
- ```java
  public void traverse(ListNode root){
	  if(root==null) return ;
	  //访问左、右子树
	  traverse(root.left);
	  traverse(root.right);
  }
  ```
- 求解原问题&寻找递归出口：
- ```java
  LinkedList<Integer> path=new LinkedList<>();
  LinkedList<String> result=new LinkedList<>();
  public void solution(TreeNode root){
	  traverse(root);
  }
  
  public void traverse(TreeNode root){
	  //常规基准条件判断
	  if(root==null) return;
	  //前序
	  path.addLast(root.val);
	  //原问题求解——末端条件判断
	  if(root.left==null&&root.right==null){
		  result.addLast(String.join("->",path));
	  }
	  traverse(root.left);
	  traverse(root.right);
	  //后序
	  path.removeLast();
  }
  ```

#### 根节点到叶节点数字之和
- 结果类型判断：自上往下
- 子问题分解：子问题的递归遍历表述
- 原问题求解&基准条件作为递归出口
- 核心代码：
- ```java
  StringBuilder path=new StringBuilder();
  int res=0;
  public int solution(TreeNode root){
	  traverse(root);
	  return res;
  }
  public void traverse(TreeNode root){
	  if(root==null) return;
	  
	  //pre
	  path.append(root.val);
	  //叶子节点
	  if(root.left==null&&root.right==null){
		  res+=Integer.parseInt(path.toString())
	  }
	  traverse(root.left);
	  traverse(root.right);
	  path.deleteCharAt(path.length()-1);
  }
  ```