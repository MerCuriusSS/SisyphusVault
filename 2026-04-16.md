## 9:00-10:00：
### 反转链表回顾
#### 小结：
反转链表：
1. 消除思维误区，反转实质是将当前节点变为【反转后链表的头节点】
2. 反转逻辑：先指后，再向前；nxt处要判空
>cur.next=pre;
>pre=cur;
>cur=nxt;
>if(nxt!=null) nxt=nxt.next;

## 10:10-11:10
### 最左前缀匹配原则
- **是什么**？联合索引的匹配规则——从左往右，连续不跳步
- **为什么**?底层按照字段顺序排序。（a,b,c）的排列顺序是先排a，a相同再排b，b相同再排c。跳过前面的排序，数据库就无法按照既定结构定位，需要全表扫描。目的是为了减少IO，提高查询效率。
- **谁在用**？B+树存储结构的关系型数据库（Mysql、PGSQL）
- **什么时候生效**？
	- 查询条件包含从左往右连续字段时
	- 出现范围查询时，后续字段索引失效
- **在哪里用**？经常需要固定多条件查询的情况——省份&城市&区域/订单&人员&状态等
- **怎么用**？
	- 建立索引时根据使用频率、区分度高按从左往右的顺序建立
	- 查询条件遵循从左往右连续字段的顺序
	- 范围条件尽量放在索引末尾字段
	- 不使用select `*`

#### 小结：
最左前缀匹配原则：
- 是什么？联合索引的匹配规则——从左到右、连续不跳步
- 为什么？减少IO，提高查找效率——底层通过从左到右的排列顺序，先排a，a相同再排b，b相同再排c，以此类推。
- 谁在用？使用B+树存储结构的关系型数据库（MySQL）
- 在哪用？频繁使用固定多条件查询的情况
- 什么时候生效？查询字段满足从左到右连续字段时；出现范围查询时，后续字段索引失效
- 怎么用？
	- 建立索引时遵循最常用、最有区分度的优先级从左往右建立。
	- 范围查询尽量往索引末尾靠
	- 查询按照从左往右连续顺序条件查询
	- 不用select `*`


## 13:00-18:30
### AI项目对话历史模块
- 现状：当前设计中AI对话框 无上下文记忆；如何敲定上下文内容的存储介质
- 方案：
	- 开启上下文记忆模块：chatMemory
	- 确定存储介质：redis临时存储、响应快；mysql持久化存储、不丢失
	- 记忆分页查询：游标查询代替传统分页查询——按「最新时间」下拉滚动翻页
- 流程：[历史对话模块流程图](excalidraw/历史对话模块流程图.md)


### 优化输出内容：生成工程化项目（vite+vue3）
- 现状：
	- 生成的内容简单：html+css+js，不契合现代前端项目工程化构建的标准
		- 核心框架：VUE、react
		- 构建工具：Vite、webpack
		- UI组件库：
		- 包管理工具
	- 