```mindmap
  root((Java 性能优化))
    0. 总体方法论：先定位，再优化，最后验证
      回答要点
        不是凭感觉改代码
        基于指标定位瓶颈
        明确目标：RT/QPS/CPU/GC/数据库压力
        通过监控、日志、压测、链路追踪定位
        分层优化：数据库/缓存/JVM/代码/并发/网络/架构
        用同样压测模型和线上指标验证
      常见目标
        接口响应时间从 2s 降到 300ms
        P95/P99 响应时间下降
        QPS 提升
        慢 SQL 减少
        CPU 使用率下降
        Full GC 次数减少
        Redis 命中率提升
        MQ 堆积减少
      工具/指标
        压测：JMeter/wrk/ab/Gatling/K6
        监控：Prometheus/Grafana/Micrometer/Actuator
        链路追踪：SkyWalking/Zipkin/Jaeger/Pinpoint
        日志：traceId/接口耗时/慢 SQL/错误日志
        JVM：GC 日志/jstat/jmap/jstack/Arthas/VisualVM/MAT
        数据库：慢查询日志/explain/show processlist
        Profiling：async-profiler/火焰图/JFR
      串联方式
        先说方法论，避免直接堆手段
        先看链路耗时
        定位到数据库/JVM/线程池/外部服务
        按瓶颈所在层展开优化
        用 P95/P99/QPS/错误率/资源使用率验证
    1. 数据库优化：最常见的后端性能瓶颈
      回答要点
        很多接口慢根因在数据库访问
        先看 SQL 是否慢
        看索引是否命中
        看扫描行数是否过大
        排查 N+1/大事务/深分页/连接池不足
        优化前先看执行计划
      常见问题
        慢 SQL
        全表扫描
        索引失效
        N+1 查询
        深分页慢
        join 过重
        select * 返回字段过多
        单次查询数据量过大
        大事务/长事务
        锁等待/死锁
        数据库连接池打满
      具体优化手段
        SQL 优化
          避免 select *
          只查询必要字段，使用 DTO 投影
          避免 where 字段上使用函数
          避免隐式类型转换
          避免不必要的 order by/group by
          减少复杂 join，必要时拆分查询
          批量查询代替循环单条查询
        索引优化
          为 where/join/order by 高频字段建索引
          根据查询条件设计联合索引
          遵守最左前缀原则
          高区分度字段更适合建索引
          使用覆盖索引减少回表
          避免索引过多影响写入
          清理无效或重复索引
        分页优化
          小数据量 limit offset
          大 offset 改游标分页
          where id > lastId order by id limit size
          先查主键 ID，再回表
          搜索型分页交给 Elasticsearch
        ORM/JPA/MyBatis 优化
          解决 N+1 查询
          使用 join fetch 或批量查询
          控制懒加载触发时机
          分页查询避免全量加载
          批量插入/批量更新
          避免循环里调用 mapper/repository
        事务优化
          缩小事务范围
          避免事务中调用远程服务
          避免事务中大量计算或文件 IO
          降低锁持有时间
          合理选择事务隔离级别
          拆分大事务
        架构级数据库优化
          读写分离
          分库分表
          冷热数据分离
          历史数据归档
          预聚合统计结果
          报表查询走 OLAP 或离线任务
      工具/指标
        慢 SQL 日志
        explain 执行计划
        rows 扫描行数
        type 访问类型
        possible_keys/key 是否命中索引
        Extra：filesort/temporary
        数据库 CPU/IO/连接数
        HikariCP 活跃连接数/等待连接数
        MyBatis/JPA SQL 日志
      串联方式
        数据库耗时高先看慢 SQL 和 explain
        扫描行数大就考虑索引和 SQL 改写
        SQL 次数多就排查 N+1 和循环查库
        读压力高且读多写少可引入缓存
        单库压力过大再考虑读写分离/分库分表
    2. 缓存优化：降低数据库压力和接口响应时间
      回答要点
        适合读多写少、热点明显、允许最终一致
        目标是减少数据库访问、降低 RT、提升吞吐
        不能无脑加缓存
        要考虑一致性、失效策略和异常场景
        讲清穿透、击穿、雪崩、热点 Key
      常见问题
        热点数据频繁查数据库
        数据库读压力过高
        接口响应慢
        相同计算结果重复生成
        缓存命中率低
        缓存穿透
        缓存击穿
        缓存雪崩
        热点 Key 打爆 Redis 单节点
        缓存与数据库不一致
      具体优化手段
        缓存设计
          Redis 分布式缓存
          Caffeine/Guava 本地缓存
          本地缓存 + Redis 多级缓存
          缓存预热
          缓存定时刷新
          热点数据常驻缓存
          合理设置 TTL
        缓存穿透
          参数合法性校验
          布隆过滤器
          缓存空值
          恶意请求限流
        缓存击穿
          互斥锁
          逻辑过期
          异步刷新
          热点 Key 不设置物理过期
          单飞机制避免同时回源
        缓存雪崩
          TTL 加随机值
          错开过期时间
          Redis 高可用
          本地缓存兜底
          限流降级
          熔断保护数据库
        热点 Key
          热点 Key 拆分
          本地缓存抗热点
          读请求副本分摊
          提前识别热点数据
          限制单 Key 高频访问
        一致性
          先更新数据库，再删除缓存
          删除缓存失败要重试
          延迟双删
          MQ 异步删除或刷新缓存
          订阅 binlog 同步缓存
          根据业务决定强一致或最终一致
      工具/指标
        Redis 命中率
        Redis QPS
        Redis slowlog
        Redis 内存使用
        大 Key/热 Key
        缓存 key 数量
        数据库 QPS 是否下降
        接口 P95/P99 是否下降
        缓存回源次数
      串联方式
        数据库读压力高且读多写少时考虑缓存
        加缓存前判断是否允许短暂不一致
        加缓存后监控命中率
        命中率低分析 key/TTL/访问模式
        缓存异常要有限流、降级或本地兜底
    3. JVM 优化：关注 GC、内存和线程状态
      回答要点
        不是一上来调参数
        先看 GC、内存、线程、对象分配
        关注 Full GC、内存泄漏、对象创建过多、线程阻塞
        先定位原因，再代码优化或参数调整
        通常和对象生命周期、缓存大小、线程池配置有关
      常见问题
        Full GC 频繁
        Stop The World 时间长
        Old 区持续增长
        内存泄漏
        大对象频繁分配
        对象创建过多
        元空间溢出
        线程阻塞
        死锁
        CPU 飙高
      具体优化手段
        GC 优化
          分析 Young GC/Mixed GC/Full GC
          减少短时间大量对象创建
          避免大对象频繁进入老年代
          控制缓存、集合、队列大小
          修复对象无法释放
          合理设置堆大小
          选择 G1/ZGC 等收集器
        内存泄漏优化
          dump 堆内存
          分析大对象和引用链
          检查静态集合未清理
          检查 ThreadLocal 未 remove
          检查监听器/回调/缓存未释放
          限制本地缓存和队列容量
        CPU 飙高优化
          top 定位高 CPU 进程
          top -H 定位高 CPU 线程
          jstack 查看线程栈
          转换线程 ID 找 Java 线程
          排查死循环/频繁 GC/复杂正则/热点计算
          用火焰图分析热点方法
        参数优化
          -Xms/-Xmx 设置堆大小
          -XX:MaxMetaspaceSize 设置元空间上限
          启用 GC 日志
          设置 OOM 自动 dump
          G1 关注 MaxGCPauseMillis
          参数调整必须压测验证
      工具/指标
        jstat：GC 次数和耗时
        jmap：dump 堆内存
        jstack：线程栈
        jcmd：JVM 信息和诊断命令
        Arthas：trace/watch/thread/heapdump
        VisualVM
        MAT
        JFR
        async-profiler
        GC 日志
        指标：堆使用率/Full GC/GC pause/对象分配速率/线程数
      串联方式
        整体抖动看 JVM 和系统资源
        P99 偶尔很高看 GC/线程阻塞/外部超时
        Old 区持续上涨就 dump 分析引用链
        CPU 高结合线程栈和火焰图
        JVM 参数调整放最后
    4. 代码层优化：减少重复工作和无效消耗
      回答要点
        核心是减少无效工作
        看循环查库、重复计算、不合理集合、大对象创建、阻塞调用
        高频接口关注序列化、对象拷贝、日志打印
        关注复杂度、数据量和调用次数
      常见问题
        循环里查数据库
        循环里调 RPC
        重复计算
        List contains 导致 O(n²)
        大集合一次性加载
        大文件一次性读取
        Bean copy 过度使用
        JSON 序列化对象过大
        日志打印大对象
        正则表达式性能差
        同步阻塞代码过多
      具体优化手段
        批量化
          批量查询代替循环查询
          批量插入代替逐条插入
          批量 RPC 代替多次 RPC
          批量组装减少往返
        降低复杂度
          List contains 改 HashSet contains
          双重循环改 Map 分组
          提前构建索引 Map
          避免重复排序
          避免重复过滤
        减少对象和内存压力
          避免无意义临时对象
          控制集合初始容量
          大文件流式处理
          分页处理大数据
          减少深拷贝
          谨慎使用 BeanUtils 高频反射拷贝
        序列化和日志
          只返回必要字段
          避免返回大对象树
          高频日志降级或采样
          日志参数化
          避免打印超大 JSON
          必要时优化 JSON 序列化配置
      工具/指标
        Arthas trace
        Arthas watch
        Arthas monitor
        async-profiler
        JFR
        JMH
        方法耗时日志
        指标：方法耗时/调用次数/对象分配率/内存占用
      串联方式
        SQL 不慢但接口慢就看应用内部耗时
        方法调用多就考虑批量化或缓存
        CPU 高用火焰图看热点代码
        内存压力大看对象分配和集合大小
        优化后再看 RT 和资源指标
    5. 并发与线程池优化：控制资源，而不是盲目加线程
      回答要点
        线程池用于复用线程和控制并发
        不是线程越多越好
        CPU 密集型和 IO 密集型线程数不同
        监控活跃线程、队列、拒绝、任务耗时
        锁优化重点是缩小锁粒度
        配合限流、隔离和降级
      常见问题
        线程池队列堆积
        线程数过多导致上下文切换
        慢任务占满线程池
        拒绝策略不合理
        锁竞争严重
        死锁
        同步等待多个外部结果
        异步任务没有资源隔离
        CompletableFuture 共用公共线程池
      具体优化手段
        线程池配置
          根据任务类型设置核心线程数
          CPU 密集型接近 CPU 核数
          IO 密集型可适当增大线程数
          设置有界队列
          设置合理拒绝策略
          不同业务独立线程池
          参数结合压测调整
        异步化
          非核心流程异步
          日志/通知/短信/邮件异步
          独立远程调用并行
          CompletableFuture 指定自定义线程池
          注意超时控制
          注意异常处理和结果回收
        锁优化
          缩小锁范围
          降低锁粒度
          避免锁内调用数据库或 RPC
          读多写少使用读写锁
          高频计数使用 LongAdder
          使用无锁数据结构
          分段锁降低竞争
        资源隔离
          核心和非核心线程池隔离
          不同下游依赖调用池隔离
          慢任务和快任务隔离
          设置超时和熔断
          防止一个依赖拖垮服务
      工具/指标
        活跃线程数
        队列长度
        任务执行耗时
        任务等待时间
        拒绝次数
        jstack 线程状态
        Arthas thread
        CPU 上下文切换
        锁等待时间
        死锁检测
      串联方式
        请求堆积看线程池是否打满
        打满后判断线程数/队列/任务慢
        任务慢继续查数据库/RPC/锁竞争
        外部依赖慢做超时、熔断、隔离
        非核心阻塞主链路可异步化或 MQ 化
    6. 接口与远程调用优化：减少链路耗时和故障放大
      回答要点
        微服务接口慢常来自远程调用和链路过长
        关注调用次数、单次耗时、超时、重试、连接池、响应体大小
        外部调用必须设置超时
        重试要有限制并配合熔断
        非核心调用异步化，核心调用保稳定
      常见问题
        一个接口调用多个下游
        串行调用导致耗时累加
        下游偶发超时
        重试放大流量
        响应数据过大
        HTTP 连接池不足
        未设置超时时间
        第三方接口拖慢主流程
        网关或服务间链路过长
      具体优化手段
        接口设计
          减少返回字段
          分页返回
          批量接口减少多次请求
          合并强相关接口
          避免返回过深对象结构
          使用压缩降低大响应传输成本
        远程调用
          设置连接超时和读取超时
          设置连接池
          合理配置最大连接数
          独立下游并行调用
          下游慢时快速失败
          第三方接口隔离
        稳定性保护
          限流
          熔断
          降级
          舱壁隔离
          重试退避
          幂等设计
          防止重试风暴
      工具/指标
        链路追踪每段耗时
        下游接口 P95/P99
        超时次数
        重试次数
        熔断次数
        降级次数
        HTTP 连接池使用率
        网关耗时
        请求体/响应体大小
        错误率
      串联方式
        下游耗时高看下游 RT 和超时配置
        多个下游互不依赖可并行
        下游不稳定加超时、熔断、降级
        非核心第三方依赖可异步化或 MQ 化
        响应体太大就优化 DTO 和分页
    7. MQ 与异步化优化：削峰、解耦、保护主链路
      回答要点
        MQ 不一定让单个任务更快
        能削峰、解耦、提升吞吐和稳定性
        适合非实时、最终一致、可异步场景
        关注可靠性、幂等、顺序、重复消费和堆积
        核心是缩短主链路
      常见问题
        高峰流量直接打数据库
        下单链路非核心操作过多
        写日志/通知/短信阻塞主流程
        突发请求压力过大
        消费端处理不过来
        消息重复消费
        消息丢失
        异步后最终一致性问题
      具体优化手段
        异步削峰
          请求先写 MQ
          消费端按能力处理
          控制消费者并发
          高峰流量排队
          保护数据库和下游
        主链路缩短
          发短信异步
          积分/优惠券异步
          操作日志异步写入
          通知类任务异步
          报表统计离线处理
        可靠性设计
          生产端确认机制
          消费端手动 ack
          消费失败重试
          死信队列
          本地消息表
          事务消息
          消费幂等
          消息状态补偿任务
      工具/指标
        MQ 堆积量
        消费延迟
        生产 TPS
        消费 TPS
        消费失败次数
        重试次数
        死信队列数量
        消费者线程数
        业务最终一致性校验
      串联方式
        主链路有非核心慢操作可异步化
        流量有峰值可用 MQ 削峰
        异步后补充幂等、重试、补偿
        消息堆积看消费能力、下游瓶颈、消费者并发
        MQ 是稳定性和吞吐优化，不适合所有场景
    8. 架构层优化：当单点优化不够时扩大系统能力
      回答要点
        单机/单库/单服务到瓶颈才做架构优化
        包括扩容、拆分、限流、降级、读写分离、分库分表、冷热分离
        架构优化会引入复杂度
        要基于瓶颈和增长判断是否值得
        高并发系统依赖多层保护
      常见问题
        单机 CPU 或内存瓶颈
        单库读写压力过高
        单表数据量过大
        高峰流量超过承载
        下游故障影响核心链路
        历史数据拖慢在线查询
        报表统计影响交易库
        服务职责过重导致扩展困难
      具体优化手段
        水平扩容
          增加应用实例
          负载均衡分摊流量
          服务无状态化
          Session 外置到 Redis
          K8s HPA 自动扩缩容
        数据架构
          读写分离
          分库分表
          冷热数据分离
          历史数据归档
          Elasticsearch 支撑复杂搜索
          OLAP 支撑报表分析
          预计算降低实时计算压力
        流量治理
          网关限流
          服务限流
          用户级限流
          热点参数限流
          熔断降级
          黑白名单
          流量预案
        服务拆分
          按业务边界拆分
          核心和非核心链路拆分
          读服务和写服务分离
          查询模型和写入模型分离
          避免过度拆分导致链路变长
      工具/指标
        服务实例 CPU/内存
        单实例 QPS
        负载均衡分布
        数据库主从延迟
        单表数据量
        分片热点
        网关 QPS
        限流次数
        熔断降级次数
        容量评估报告
      串联方式
        单机资源打满先水平扩容
        扩容后数据库成瓶颈则优化数据库或加缓存
        单库到瓶颈再考虑读写分离和分库分表
        突发流量高要限流、削峰、降级
        架构优化要考虑收益和复杂度
    9. 验证与复盘：证明优化真的有效
      回答要点
        必须有前后对比
        不能只说感觉快了
        用指标证明
        关注平均值，也关注 P95/P99
        确认没有引入新问题
        沉淀监控、告警和容量评估
      验证指标
        QPS
        平均响应时间
        P95/P99 响应时间
        错误率
        CPU 使用率
        内存使用率
        Full GC 次数和停顿时间
        慢 SQL 数量
        数据库 CPU 和连接数
        Redis 命中率
        线程池队列长度
        MQ 堆积量
        下游超时次数
      验证方式
        相同压测模型前后对比
        灰度发布观察指标
        线上监控观察一段时间
        回归测试确认功能正确
        核对业务数据一致性
        保留回滚方案
      串联方式
        优化前记录基线指标
        优化后用同样流量模型压测
        P99 下降但错误率上升说明优化不完整
        接口变快但压力转移到 Redis 也要评估
        最后形成监控和告警
```