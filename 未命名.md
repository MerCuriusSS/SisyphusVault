模块层级结构分析

  1️⃣ 根父模块：ruoyi-vue-plus

  - 作用：顶层聚合 POM，统一管理所有子模块
  - 职责：
    - 版本统一管理（当前版本：5.5.2）
    - 依赖版本管理
    - Maven 插件配置
    - 编译规范配置（Java 16+）

  2️⃣ 第一层子模块（4个聚合模块）：

  | 模块          | 类型 | 说明                                    |
  |---------------|------|-----------------------------------------|
  | ruoyi-admin   | JAR  | 主应用入口模块，包含 Spring Boot 启动类 |
  | ruoyi-common  | POM  | 通用模块聚合器，包含 24 个子模块        |
  | ruoyi-modules | POM  | 业务模块聚合器，包含 5 个业务模块       |
  | ruoyi-extend  | POM  | 扩展服务聚合器，包含 2 个扩展服务       |

  3️⃣ 核心依赖链（ruoyi-common 的层级关系）：

  底层 → 顶层：
  common-core (最底层)
      ↓
  common-json
      ↓
  common-redis
      ↓
  common-satoken → common-mybatis
      ↓                                    ↓
  common-security   common-tenant
      ↓                                    ↓
  业务模块依赖 ← ← ← ← ← ←

  关键依赖说明：
  - common-core: 最底层，只依赖 Spring 框架和第三方工具类（Hutool、Lombok、MapStruct）
  - common-redis: 依赖 core，提供 Redis 缓存功能
  - common-satoken: 依赖 core 和 redis，提供 Sa-Token 认证
  - common-mybatis: 依赖 core 和 satoken，提供数据库操作
  - common-tenant: 依赖 mybatis 和 redis，提供多租户功能
  - common-security: 依赖 satoken，提供安全控制
  - common-web: 依赖 json，提供 Web 服务基础


  模块分类总结

  🔴 核心公共模块（最底层，被其他所有模块依赖）

  - ruoyi-common-core: 提供基础工具类、常量、异常处理、验证注解等

  🟡 基础服务模块（构建在核心模块之上）

  - ruoyi-common-json: JSON 序列化（Jackson）
  - ruoyi-common-redis: Redis 缓存服务
  - ruoyi-common-satoken: Sa-Token 认证框架
  - ruoyi-common-mybatis: MyBatis-Plus 数据库操作
  - ruoyi-common-web: Web 服务基础

  🟢 功能增强模块（依赖基础服务）

  - ruoyi-common-tenant: 多租户支持
  - ruoyi-common-security: 安全控制
  - ruoyi-common-log: 日志记录
  - ruoyi-common-excel: Excel 导入导出
  - ruoyi-common-oss: 对象存储
  - ruoyi-common-sms: 短信服务
  - ruoyi-common-mail: 邮件服务
  - 其他功能模块...

  🔵 业务模块（最终实现）

  - ruoyi-system: 系统管理核心模块（用户、角色、菜单、部门等）
  - ruoyi-job: 定时任务管理
  - ruoyi-generator: 代码生成器
  - ruoyi-demo: 演示模块
  - ruoyi-workflow: 工作流模块

  🟣 应用入口模块

  - ruoyi-admin: Spring Boot 应用启动入口，聚合所有业务模块和通用模块，最终打包为可执行 JAR

  这种模块化设计的优点是：
  1. 层次清晰：核心 → 基础 → 增强 → 业务
  2. 职责单一：每个模块只负责特定功能
  3. 低耦合：通过依赖管理避免循环依赖
  4. 易扩展：新模块可以按需引入依赖
  5. 易维护：修改某个功能只需更新对应的 common 模块


---

● 详细对比分析

  📋 pom.xml 依赖关系对比

  ruoyi-common-satoken/pom.xml

  <dependency>
      <groupId>org.dromara</groupId>
      <artifactId>ruoyi-common-core</artifactId>
  </dependency>
  ? for shortcuts
  <!-- RuoYi Common Redis -->
  <dependency>n 内部依赖关系
      <groupId>org.dromara</groupId>
      <artifactId>ruoyi-common-redis</artifactId>
      <!-- 注意：没有 <optional>true</optional> -->
  </dependency>> SaToken
      Common --> MyBatis
  <dependency>-> Tenant
      <groupId>cn.dev33</groupId>
      <artifactId>sa-token-spring-boot3-starter</artifactId>
  </dependency>> Doc
      Common --> Excel
  <dependency>-> Idempotent
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
  </dependency>> Mail
      Common --> OSS
  ruoyi-common-redis/pom.xml
      Common --> SMS
  <dependency>-> Social
      <groupId>org.dromara</groupId>
      <artifactId>ruoyi-common-core</artifactId>
  </dependency>> Encrypt
      Common --> WebSocket
  <!--redisson-->SSE
  <dependency>-> BOM
      <groupId>org.redisson</groupId>
      <artifactId>redisson-spring-boot-starter</artifactId>
  </dependency>Core
      Redis --> Core
  <dependency>--> Core
      <groupId>com.baomidou</groupId>
      <artifactId>lock4j-redisson-spring-boot-starter</artifactId>
  </dependency>-> SaToken
      Tenant --> MyBatis
  --- Tenant --> Redis
  ❌ 核心结论：删除 Redis 模块会怎样？
      Web --> Json
  无法独立编译！运行时也会失败！
      %% 业务模块
  原因分析：[ruoyi-system<br/>系统管理模块]:::businessModule
      JobModule[ruoyi-job<br/>定时任务模块]:::businessModule
  1️⃣ 编译时失败uoyi-generator<br/>代码生成器]:::businessModule
      Demo[ruoyi-demo<br/>演示模块]:::businessModule
  // PlusSaTokenDao.java 第 7 行/>工作流模块]:::businessModule
  import org.dromara.common.redis.utils.RedisUtils;  // ← 直接引用了 redis 模块的类
      Modules --> System
  如果删除 ruoyi-common-redis 模块，编译时会报错：
  [ERROR] /ruoyi-common-satoken/.../PlusSaTokenDao.java:[7,33]
         找不到符号: 程序包org.dromara.common.redis.utils
      Modules --> Workflow
  2️⃣ 代码层面强依赖
      %% System 模块依赖
  整个 PlusSaTokenDao.java 文件中有 20+ 处直接调用 RedisUtils 的代码：
      System --> Doc
  // 第 40 行：获取值tis
  Object o = CAFFEINE.get(key, k -> RedisUtils.getCacheObject(key));
      System --> OSS
  // 第 54 行：设置永不过期
  RedisUtils.setCacheObject(key, value);
      System --> SMS
  // 第 56 行：设置带过期时间
  RedisUtils.setCacheObject(key, value, Duration.ofSeconds(timeout));
      System --> Web
  // 第 66 行：判断键是否存在
  if (RedisUtils.hasKey(key)) {
      RedisUtils.setCacheObject(key, value, true);
  }   System --> WebSocket
      System --> SSE
  // 第 77 行：删除键
  if (RedisUtils.deleteObject(key)) {
      CAFFEINE.invalidate(key);in<br/>监控中心]:::businessModule
  }   SnailJob[ruoyi-snailjob-server<br/>任务调度服务器]:::businessModule

  // 第 87 行：获取剩余存活时间
  long timeout = RedisUtils.getTimeToLive(key);

  // 第 97 行：更新过期时间终打包）
  RedisUtils.expire(key, Duration.ofSeconds(timeout));
      Admin -.-> Social
  // 第 187 行：模糊查询iter
  Collection<String> keys = RedisUtils.keys(keyStr);
      Admin -.-> System
  --- Admin -.-> JobModule
  🔍 实际采用的解耦策略tor
      Admin -.-> Demo
  ❌ 不是 通过 Maven 可选依赖解耦

  这个项目没有使用 Maven 的 <optional>true</optional> 来解耦：
      subgraph 图例
  <!-- 如果使用可选依赖，应该是这样的（但实际没有）-->
  <dependency>合模块]:::aggregator
      <groupId>org.dromara</groupId>
      <artifactId>ruoyi-common-redis</artifactId>
      <optional>true</optional>  <!-- ← 实际上没有这个 -->
  </dependency>用入口]:::appModule
      end
  ✅ 而是 通过 Sa-Token 接口抽象 + 强绑定实现
  模块分类总结
  Sa-Token 框架本身提供了接口抽象：
  // Sa-Token 提供的接口（抽象层）有模块依赖）
  public interface SaTokenDao {
      String get(String key);础工具类、常量、异常处理、验证注解等
      void set(String key, String value, long timeout);
      void delete(String key);块之上）
      // ... 其他方法
  } ruoyi-common-json: JSON 序列化（Jackson）
  - ruoyi-common-redis: Redis 缓存服务
  RuoYi 的实现策略：oken: Sa-Token 认证框架
  // RuoYi 提供的实现（具体绑定 Redis）数据库操作
  public class PlusSaTokenDao implements SaTokenDaoBySessionFollowObject {
      // 强绑定 RedisUtils
      @Override（依赖基础服务）
      public String get(String key) {
          Object o = CAFFEINE.get(key, k -> RedisUtils.getCacheObject(key));
          return (String) o;全控制
      }yi-common-log: 日志记录
  } ruoyi-common-excel: Excel 导入导出
  - ruoyi-common-oss: 对象存储
  ---uoyi-common-sms: 短信服务
  🏗️ 架构设计：多级缓存策略务
  - 其他功能模块...
  从 PlusSaTokenDao.java 第 26-33 行可以看到：
  🔵 业务模块（最终实现）
  private static final Cache<String, Object> CAFFEINE = Caffeine.newBuilder()
      // 设置最后一次写入或访问后经过固定时间过期单、部门等）
      .expireAfterWrite(5, TimeUnit.SECONDS)  // 5秒过期
      // 初始的缓存空间大小成器
      .initialCapacity(100)
      // 缓存的最大条数流模块
      .maximumSize(1000)
      .build();

  两级缓存架构： Spring Boot 应用启动入口，聚合所有业务模块和通用模块，最终打包为可执行 JAR
  ┌───────���─────────────────────────────────┐
  │         PlusSaTokenDao                  │
  ├─────────────────────────────────────────┤
  │  L1: Caffeine (本地内存缓存)            │  ← 5秒过期，快速访问
  │  ↓ miss                                  │
  │  L2: Redis (分布式缓存)                 │  ← 长期存储，分布式共享
  └─────────────────────────────────────────┘n 模块

  查询流程（第 40 行）：
  Object o = CAFFEINE.get(key, k -> RedisUtils.getCacheObject(key));
  //              ↑ 本地缓存       ↑ 如果本地缓存未命中，从 Redis 加载

  写入流程（第 54-58 行）：
  RedisUtils.setCacheObject(key, value);  // 写入 Redis
  CAFFEINE.invalidate(key);                // 清空本地缓存，下次读取会重新从 Redis 加载

  ---
  📊 依赖关系总结图

  graph TB
      subgraph "依赖关系"
          Core[ruoyi-common-core]:::base
          Redis[ruoyi-common-redis]:::redis
          SaToken[ruoyi-common-satoken]:::satoken
          MyBatis[ruoyi-common-mybatis]:::mybatis
      end

      Core --> Redis
      Redis --> SaToken
      Core --> SaToken

      SaToken -.-> MyBatis

      subgraph "强依赖证据"
          RedisUtils[RedisUtils 类]:::code
          Import[import RedisUtils]:::code
          Call[调用 RedisUtils.XXX]:::code
      end

      Redis --> RedisUtils
      SaToken --> Import
      Import --> Call

      classDef base fill:#e3f2fd,stroke:#1976d2
      classDef redis fill:#ffeb3b,stroke:#f57f17
      classDef satoken fill:#4caf50,stroke:#2e7d32
      classDef mybatis fill:#ff9800,stroke:#e65100
      classDef code fill:#f44336,stroke:#c62828,color:#fff

  ---
  
  💡 如果要实现真正的解耦，该怎么做？

  方案 1：使用 Maven 可选依赖（不推荐）

  <!-- ruoyi-common-satoken/pom.xml -->
  <dependency>
      <groupId>org.dromara</groupId>
      <artifactId>ruoyi-common-redis</artifactId>
      <optional>true</optional>  <!-- 标记为可选 -->
  </dependency>

  问题：代码层面仍然 import，编译还是失败！

  方案 2：接口抽象 + 多实现（推荐）

  // 1. 定义接口
  public interface CacheService {
      String get(String key);
      void set(String key, String value, long timeout);
  }

  // 2. Redis 实现
  @Service
  @ConditionalOnClass(RedisUtils.class)  // 只有 Redis 存在时才生效
  public class RedisCacheService implements CacheService {
      public String get(String key) {
          return RedisUtils.getCacheObject(key);
      }
  }

  // 3. 内存实现
  @Service
  public class MemoryCacheService implements CacheService {
      private final Map<String, String> cache = new ConcurrentHashMap<>();
      public String get(String key) {
          return cache.get(key);
      }
  }

  // 4. SaTokenDao 使用接口
  public class PlusSaTokenDao implements SaTokenDao {
      @Autowired
      private CacheService cacheService;  // 注入接口，不依赖具体实现

      @Override
      public String get(String key) {
          return cacheService.get(key);
      }
  }

  ---
  🎯 最终答案

  | 问题                          | 答案                                               |
  |-------------------------------|----------------------------------------------------|
  | 删除 Redis 模块能独立编译吗？ | ❌ 不能，编译时就会报错                            |
  | 使用了可选依赖吗？            | ❌ 没有，是直接强依赖                              |
  | 如何实现解耦？                | 通过 Sa-Token 的接口抽象 + RuoYi 强绑定 Redis 实现 |
  | 设计模式？                    | 多级缓存（Caffeine L1 + Redis L2）                 |
  | 依赖类型                      | 编译时依赖 + 运行时依赖（强耦合）                  |

  RuoYi-Vue-Plus 的设计选择了简单直接的方案：明确要求 Redis 作为必需依赖，避免了多实现的复杂性，适合大多数企业级应用场景。