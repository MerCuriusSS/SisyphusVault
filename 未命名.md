<%*
// 1. 弹出输入框，让你输入任意文字
const testText = await tp.system.prompt("请输入任意测试文字（比如：Templater测试）");

// 2. 获取当前日期（Templater内置变量）
const today = tp.date.now("YYYY-MM-DD");

// 3. 渲染内容到笔记中
tR.set(`
# Templater 测试结果
- 你输入的文字：${testText}
- 当前日期（自动生成）：${today}
- Templater 状态：✅ 插件正常工作（能看到这行说明渲染成功）
`);
%>
