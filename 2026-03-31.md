## 9:30-10:30
### 1.算法复习
- 合并有序链表
- ```java
  public ListNode mergeList(ListNode l1,ListNode l2){
	  ListNode p1=l1;
	  ListNode p2=l2;
	  ListNode dummy=new ListNode(-1);
	  ListNode p=dummy;
	  
	  //核心1：新建而不是修改
	  //核心2：推进值较小的链表
	  //核心3：连接剩余链表
	  
	  while(p1!=null&&p2!=null){
		  if(p1.val<p2.val){
			  p.next=p1;
			  p1=p1.next;
		  }else{
			  p.next=p2;
			  p2=p2.next;
		  }
		  
		  p=p.next;
	  }
	  
	  if(p2!=null){
		  p.next=p2;
	  }
	  if(p1!=null){
		  p.next=p1;
	  }
	  
	  return dummy.next;
  }
  ```
  - 反转链表
  - ```java
    public ListNode reverseList(ListNode l){
	    //核心1：从头反转
	    //核心2：划分3部分：未反转、待反转、已反转
	    //核心3：先找未反转，再反转，后衔接
	    ListNode pre=null;
	    ListNode cur=l;
	    while(cur!=null){
		    ListNode next=cur.next;
		    cur.next=pre;
		    pre=cur;
		    cur=next;
	    }
	    
	    return pre;
    }
    ```
### 2.算法学习：快慢指针——链表成环判断
- 初想法：
	- 方式1：创建2个指针，一个指针指向head不动，另一个指针每次移动，如果成环，则2个指针必然相遇
		- 问题点：头节点≠成环点，当成环点不落在头节点上，即使成环，2个指针也不会相遇。
	- 方式2：创建快慢指针，快指针每次跑2步，慢指针每次跑一步，当快指针与慢指针相遇时，表示成环，当快指针出现null时，则不成环.
	-  ```java
	  public ListNode solution(ListNode l){
		  ListNode p=l;
		  ListNode fast=p.next;//思考误区1：没有将fast与slow放在同一起跑线
		  ListNode slow=p;
		  
		  while(fast!=null){
			  //思考误区2：不能保证slow与fast指针同时走
			  if(fast.next!=null){
				  fast=fast.next.next;
			  }
			  slow=slow.next;
			  
			  if(slow==fast) return true;
		  }
		  
		  return false;
	  }
	  ```
- 