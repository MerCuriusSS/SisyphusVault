## 9:30-10:30
### 1.算法复习
- 合并有序链表
- ```java
  public ListNode mergeList(ListNode l1,ListNode l2){
	  ListNode p1=l1;
	  ListNode p2=l2;
	  ListNode dummy=new ListNode(-1);
	  ListNode p=dummy;
	  
	  //核心1：新建而不是修改
	  //核心2：推进值较小的链表
	  //核心3：连接剩余链表
	  
	  while(p1!=null&&p2!=null){
		  if(p1.val<p2.val){
			  p.next=p1;
			  p1=p1.next;
		  }else{
			  p.next=p2;
			  p2=p2.next;
		  }
		  
		  p=p.next;
	  }
	  
	  if(p2!=null){
		  p.next=p2;
	  }
	  if(p1!=null){
		  p.next=p1;
	  }
	  
	  return dummy.next;
  }
  ```
  - 反转链表
  - ```java
    public ListNode reverseList(ListNode l){
	    //核心1：从头反转
	    //核心2：划分3部分：未反转、待反转、已反转
	    //核心3：先找未反转，再反转，后衔接
	    ListNode pre=null;
	    ListNode cur=l;
	    while(cur!=null){
		    ListNode next=cur.next;
		    cur.next=pre;
		    pre=cur;
		    cur=next;
	    }
	    
	    return 
    }
    ```