## 9:30-10:30
### 一、复习快慢指针应用——环形链表
- 快慢指针定义：2条步长不一致的指针同向而行
- 环形链表中的应用、避坑点和核心代码
	- 闭坑点：1.快慢指针初始化时同一起跑线；2.成环点≠头节点；3.保证快慢指针要同时走
	- ```java
	  public boolean solution(ListNode l){
		  ListNode p=l;
		  ListNode slow=p;
		  ListNode fast=p;
		  
		  while(fast!=null&&fast.next!=null){
			  slow=slow.next;
			  fast=fast.next;
			  
			  if(slow==fast)
				  return true;
		  }
		  
		  return false;
	  }
	  ```

### 二、AI项目回顾
- 用户管理模块具体设计——用户注册、用户登录、用户登录信息、用户注销、用户权限、管理用户
- 表设计：user；关键字段设计：id（雪花算法防爬虫）、账号、密码、用户名称、用户角色；约束：id（主键索引）；账号（唯一索引）；用户名称（普通索引）
- 技术实现：mysql、mybatis-flex（三层架构代码自动生成、多表连接 核心优势）
- 业务流程设计：
	- 登录：**存疑** 
	- 浏览器与前端区别
		- 定位：前端（代码：逻辑与页面）；浏览器（带能力的运行环境）
		- 角色：前端（指导者）；浏览器（执行者）
		- 功能：前端（业务逻辑、用户交互、API调用）；浏览器（前端要求做的+协议规定主动做的【set-cookie、重定向、同源策略】）
- mybatis-flex配置
- 数据模型（角色枚举、请求参数）
- 「注册」业务逻辑开发

## 10:49-11:49
### mysql存储引擎认识与区分
- 定义：管理数据的方式
- 具体怎么管？
	- 数据怎么存？存储文件、外键
	- 数据怎么改？锁结构、事务机制、MVCC
	- 数据怎么查？索引机制；B+Tree
	- 风险与安全？崩溃恢复
- 不同存储引擎表现区别？
	- 存
		- 存储文件：
			- MyISAM：索引（.myi）、数据（.myd）分开，先索引再数据
			- innodb：索引数据同文件（.ind）
		- 外键：
			- MyISAM：不支持外键
			- innodb：支持外键
	- 查：
		- 索引结构：
			- MyISAM：非聚簇索引
			- Innodb：聚簇索引（主键+完整数据、基本数据组织形式）+二级索引（主键+普通索引）
	- 改：
		- 锁机制：
			- MyISAM：表级锁
			- Innodb：行级锁 + 表锁
		- 事务管理：
			- MyISAM：不支持事务
			- Innodb：支持事务、
		- MVCC：
			- MyISAM:不支持
			- Innodb：实现读写不阻塞，提高并发
	- 风险与安全：
		- MyISAM：没有崩溃恢复能力
		- Innodb：undolog和redolog机制。
	- 应用场景：
		- Innodb：频繁增删改、高并发、数据安全
		- MyISAM：读多写少、简单业务
- memory存储引擎：
	- 概念：将表数据保存到内存中，重启后丢失

## 14:00-15:00
### 聚簇索引与非聚簇索引的区别
- 存储内容：
	- 聚簇索引保存主键列+行数据
	- 非聚簇索引保存主键列+普通索引列
- 查询方式：
	- 聚簇索引通过主键列查询完整数据
	- 非聚簇索引需先通过索引列找到主键列，再根据主键列查询完整数据，由主键列查询完整数据的过程又称为回表操作。

### 索引类型有哪些
- 区分维度：
	- 数据结构：
		- B+索引：完整数据通过叶子节点保存，叶子节点之间形成链表。
		- hash索引：数据位置通过hash算法获取，再进行等值查询，不支持范围查询
		- 全文索引：分词倒排索引，适用于文本查询之类
	- 索引类型：
		- 主键索引：唯一且非空，一张表唯一
		- 唯一索引：值不允许重复，允许为空，一张表可以多个
		- 普通索引：没有唯一约束
		- 联合索引：2个及以上的字段组成的索引，遵循最左前缀原则
		- 全文索引：文本搜索用
	- 存储方式：
		- 聚簇索引：叶子节点保存完整行数据。
		- 非聚簇索引（二级索引）：叶子节点保存索引列和主键列。

## 15:00-16:00
### G1垃圾回收器：
- 定义：