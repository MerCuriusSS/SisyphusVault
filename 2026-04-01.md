## 9:30-10:30
### 一、复习快慢指针应用——环形链表
- 快慢指针定义：2条步长不一致的指针同向而行
- 环形链表中的应用、避坑点和核心代码
	- 闭坑点：1.快慢指针初始化时同一起跑线；2.成环点≠头节点；3.保证快慢指针要同时走
	- ```java
	  public boolean solution(ListNode l){
		  ListNode p=l;
		  ListNode slow=p;
		  ListNode fast=p;
		  
		  while(fast!=null&&fast.next!=null){
			  slow=slow.next;
			  fast=fast.next;
			  
			  if(slow==fast)
				  return true;
		  }
		  
		  return false;
	  }
	  ```

### 二、AI项目回顾
- 用户管理模块具体设计——用户注册、用户登录、用户登录信息、用户注销、用户权限、管理用户
- 表设计：user；关键字段设计：id（雪花算法防爬虫）、账号、密码、用户名称、用户角色；约束：id（主键索引）；账号（唯一索引）；用户名称（普通索引）
- 技术实现：mysql、mybatis-flex（三层架构代码自动生成、多表连接 核心优势）
- 业务流程设计：
	- 登录：与网站建立连接时