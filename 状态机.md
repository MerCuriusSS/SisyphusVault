---
tags:
  - Areas/Coder/基础原理
  - Areas/Coder/javaWeb
category: 思维
status: 加工
project:
application:
source:
---
>**笔记不是为了复述书本，而是为了**“存档当下的自己”。如果你的笔记里没有你的思考痕迹、痛苦经历和选择理由**，它就只是一份毫无生命力的说明书，自然无法在未来唤醒你的认知***


## 💥 核心结论
>核心定义是什么? 核心价值在哪里?

### 🟣 核心定义：在**特定事件触发**下，一个对象从当前状态按照**唯一确定的规则**转化为新状态的思想。
- 特定事件触发：确保对象状态不会凭空变化，需要有外部事件作为前提。
- 唯一确定的规则：确保对象同一状态不会随意变化，须有确定规则作为约束。

### 🟣 核心价值：**统一状态管理**，避免**同一状态的变化逻辑分散在各处**
## 🔪 我的见解
>为什么要记录它「我遇到了什么问题」？它能解决哪些痛点？

### 🟣 为什么要记录它？
- 存在对象随不同事件发生状态变化的场景。核心特点：一个状态可随不同事件产生不同变化（**状态使用重复**），但同一种状态在一个事件只允许一种变化（**同状态下变化事件触发唯一）**。
- “**事件驱动**”思维惯性——给定事件，输入旧状态，输出新状态【状态使用重复（事件1：A->B;事件2：A->C），**状态处理逻辑分散，修改状态需要全局改，容易遗漏，维护成本大**】


### 🟣 解决痛点：
- “**状态驱动**”思维——给定一个状态，输入事件，输出新状态【状态为原点，该状态下变化事件触发唯一】，**集中观测状态变化，不用东翻西找**。


## ⚡️ 我的重构
>它的底层逻辑是什么？（尝试用最简单的类比解释给外行听）它的结构是什么?

### 🟣 底层逻辑：以「当前状态」为第一判断条件，对「所有事件」做唯一的「状态转移」。

### 🟣 结构：
- 状态：对象的所有状态
- 事件：状态变化的诱因（输入）
- 转化：状态发生变化
- 动作：变化过程所执行的逻辑

## 🚀 实践应用：

### 🟣 最小化实践：

#### 1.统一声明状态接口，描述状态行为
```java
// 通用状态接口：适配任意事件类型
interface State {
    // 通用事件处理方法：传入事件类型，返回新状态
    State handleEvent(String event);
    // 获取状态名称（仅用于验证）
    String getName();
}
```

#### 2.实现具体状态类，封装自身的所有事件逻辑
```java
// 1. 待支付状态：处理“支付成功”“取消订单”事件，忽略其他事件
class PendingPayState implements State {
    @Override
    public State handleEvent(String event) {
        // 每个状态内部自主决定如何处理不同事件
        switch (event) {
            case "PAY_SUCCESS":
                return new PendingDeliverState(); // 支付成功→待发货
            case "CANCEL_ORDER":
                return new CanceledState();       // 取消订单→已取消
            default:
                return this; // 未知事件，状态不变
        }
    }

    @Override
    public String getName() {
        return "PENDING_PAY";
    }
}

// 2. 待发货状态：处理“发货”“申请退款”事件，忽略其他事件
class PendingDeliverState implements State {
    @Override
    public State handleEvent(String event) {
        switch (event) {
            case "DELIVER":
                return new PendingReceiptState(); // 发货→待收货
            case "APPLY_REFUND":
                return new RefundingState();      // 申请退款→退款中
            default:
                return this;
        }
    }

    @Override
    public String getName() {
        return "PENDING_DELIVER";
    }
}

// 3. 待收货状态：仅处理“确认收货”事件
class PendingReceiptState implements State {
    @Override
    public State handleEvent(String event) {
        if ("CONFIRM_RECEIPT".equals(event)) {
            return new CompletedState(); // 确认收货→已完成
        }
        return this;
    }

    @Override
    public String getName() {
        return "PENDING_RECEIPT";
    }
}
```

#### 3.通用上下文：（状态机载体）
```java
// 通用上下文：适配任意状态/事件，无需修改上下文代码
class Context {
    // 当前状态（核心变量）
    private State currentState;

    // 初始化：默认待支付状态
    public Context() {
        this.currentState = new PendingPayState();
    }

    // 通用事件处理入口：传入任意事件类型
    public void triggerEvent(String event) {
        this.currentState = this.currentState.handleEvent(event);
    }

    // 获取当前状态名称
    public String getCurrentStateName() {
        return currentState.getName();
    }
}
```

#### 4.应用案例
```java
public class GenericStatePatternSM {
    public static void main(String[] args) {
        Context order = new Context();
        
        // 初始状态：PENDING_PAY
        System.out.println(order.getCurrentStateName()); // PENDING_PAY
        
        // 触发支付成功：PENDING_PAY → PENDING_DELIVER
        order.triggerEvent("PAY_SUCCESS");
        System.out.println(order.getCurrentStateName()); // PENDING_DELIVER
        
        // 触发申请退款：PENDING_DELIVER → REFUNDING
        order.triggerEvent("APPLY_REFUND");
        System.out.println(order.getCurrentStateName()); // REFUNDING
    }
}
```
## ⛪ 场景设想
#### 适用于一切具有“明确状态”、“有触发状态变化的事件”、“目标状态变化唯一”的场景

- **场景 A**：订单业务系统，用来描述订单流转
- **场景 B**：在进行 [ZZZ] 决策时，用来规避逻辑谬误。