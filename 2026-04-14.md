## 10:30-12:30

### 链表专题
#### 单链表的中点（中间有2个节点，选第二个）
- 初认识：先遍历一次获取链表长度n，中间节点=n/2+1
- 难点：一次遍历得到链表中点
- 破局点：快慢指针同时走，慢指针每次走一步，快指针每次走两步，当快指针到尽头(null)时，慢指针指向的地方即为链表中点
- 模板代码；
- ```java
  public void findMidNode(ListNode head){
	  ListNode slow=head;
	  ListNode fast=head;
	  while(fast!=null&&fast.next!=null){
		  slow=slow.next;
		  fast=fast.next.next;
	  }
	  
	  return slow;
  }
  ```

#### 两个链表是否相交
- 初认识：链表相交=链表节点相等；同时遍历链表，链表节点相等时则表示相交，当链表结尾后仍无相同节点，则无相交
- 难点：链表长度不一，齐头并进遍历会让相交的节点无法对应。
- 破局点：将链表B尾与链表A头相连，将相交问题转化为`成环问题`,成环点就是相交点
- 模板代码：
- ```java
  public boolean getIntersectionNode(ListNode headA,ListNode headB){
	  ListNode p=headB;
	  while(p!=null&&p.next!=null){
		  p=p.next;
	  }
	  //链表B尾与链表A头相连
	  p.next=headA;
	  //判断成环
	  ListNode slow=headA;
	  ListNode fast=headA;
	  
	  while(fast!=null&&fast.next!=null){
		  slow=slow.next;
		  fast=fast.next.next;
		  
		  if(slow==fast){
			  return fast;
		  }
	  }
	  
	  return null;
  }
  ```