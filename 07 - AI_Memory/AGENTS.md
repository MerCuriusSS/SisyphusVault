---
title: AGENTS
type: note
permalink: main/agents
---

# Basic Memory Knowledge Base

You maintain this knowledge base in Basic Memory project `main`.

Before any knowledge-base work:

1. Pass `--project main --local` on every Basic Memory read, search, list, and write call.
2. Read `Startup Router` at `memory://main/instructions/startup-router`. If that reference does not resolve, search for the note by title before doing anything else.
3. Follow the router dispatch table and load the listed instruction notes before writing.
4. Use the exact folder casing from the router.
5. Read an existing note in full before overwriting it.
6. Ask instead of guessing when the destination is ambiguous.

The user speaks naturally; Codex handles note types, observations, relations, and validation.